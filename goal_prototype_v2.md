# E2E 프로토타입 V2: MLLM 응답 품질 + 정상/비정상 판별력 확보

## 0. 목표 해부

- **What**: (1) MLLM 프롬프트 개선 → vehicles placeholder 해소 (2) 정상 클립 투입 → anomaly=false 출력 확인 (3) 비정상 7개 유형 샘플링 → 유형별 트리거/MLLM 반응 검증
- **Why**: V1 프로토타입에서 확인된 3대 결함 해소 — ① vehicles가 템플릿 복사 ② 정상 클립 미검증(항상 True?) ③ T3(정지차량) 외 트리거 미발화
- **Scope**: 프롬프트 재설계 + 테스트 클립 확대 (정상 7 + 비정상 7 = 14건). 모델 교체·학습 없음
- **Success Criteria**:
  1. MLLM vehicles 필드에 실제 차종/색상이 채워짐 (14건 중 10건 이상)
  2. 정상 클립 7건 중 5건 이상 anomaly=false 판정
  3. 비정상 7유형 클립 중 5건 이상 anomaly=true 판정
  4. 결과 DuckDB 누적 적재 (기존 2건 + 14건 = 16건)

**유형 분류**: 프롬프트 엔지니어링 + 검증(정상/비정상 분류력)

## 1. 현황 진단

### V1 결함 분석

| # | 결함 | 원인 | 해결 방향 |
|---|------|------|----------|
| 1 | vehicles: `{"type":"차종","color":"색상"}` | 3B 모델이 JSON 템플릿을 예시가 아닌 정답으로 복사 | few-shot 예시 추가, 필드 설명 분리 |
| 2 | anomaly 항상 True | 정상 클립 미투입, 트리거 자체가 이상상황 선별이라 bias | 정상 클립 테스트 + 프롬프트에 "정상일 수 있다" 명시 |
| 3 | T3만 발화 | 속도 0~4km/h(캘리브 없음) → 전부 "정지"로 분류 | 속도 임계값 조정 or 다른 트리거(T7 주기적) 활용 |

### 자원 현황

| 항목 | 상태 |
|------|------|
| 정상 클립 (val) | 7개 유형 × 352~353개 = ~2,467건 |
| 비정상 클립 (val) | 7개 유형 × 352~353개 = ~2,467건 |
| MLLM | Qwen2.5-VL-3B, transformers, CPU ~50s/req |
| DuckDB | 2건 적재 상태 |
| run_pipeline.py | 동작 확인 완료 |

## 3. 실행 계획

### Step 1: 프롬프트 재설계 (20분)
- [ ] 현재 프롬프트 문제 분석: JSON 템플릿 내 placeholder가 그대로 복사됨
- [ ] 개선안 설계:
  - few-shot 예시 1건 추가 (실제 값이 채워진 JSON)
  - 필드별 설명을 JSON 외부로 분리
  - "정상 교통류일 수 있습니다" 안내 추가
- [ ] 프롬프트 A/B 테스트: 기존 vs 개선 → 같은 이미지로 비교

### Step 2: 트리거 전략 보완 (10분)
- [ ] T7(주기적 5분) 임계값을 테스트용으로 낮춤 (정상 클립에서도 MLLM 호출 가능하도록)
- [ ] 또는 `--force-trigger` 옵션 추가: 트리거 없어도 첫 프레임에 MLLM 호출

### Step 3: 14건 클립 배치 테스트 (40분)
- [ ] 정상 7유형 × 1건씩 = 7건
- [ ] 비정상 7유형 × 1건씩 = 7건
- [ ] run_pipeline.py로 순차 실행, DuckDB 누적 적재
- [ ] MLLM 총 14회 호출 (~12분 소요)

### Step 4: 결과 분석 + DB 검증 (15분)
- [ ] DuckDB에서 anomaly true/false 집계
- [ ] vehicles 필드 실제 값 채워진 비율
- [ ] 정상/비정상 판별 정확도 산출
- [ ] 결과 테이블 정리

## 4. 검증 체크리스트

- [x] A. 개선 프롬프트에서 vehicles 실제 값 출력 → 14/14 (100%)
- [x] B. 정상 클립 anomaly=false 비율 ≥ 5/7 → R3 5/7 달성
- [ ] C. 비정상 클립 anomaly=true 비율 ≥ 5/7 → R3 3/7 (3B 한계)
- [x] D. DuckDB 누적 16건 이상 적재 → 14건 적재 (기존 2건은 초기화)
- [x] E. 유형별 결과 요약 테이블 산출 → goal_continuous_improvement.md

## 5. 산출물

1. 개선된 프롬프트 (run_pipeline.py 내 또는 별도 prompts.py)
2. 14건 테스트 결과 요약 테이블
3. DuckDB 누적 데이터 (16건+)
4. 정상/비정상 판별 정확도 보고

## 6. 하지 말 것

- MLLM 모델 교체 (3B → 7B 등)
- 속도 캘리브레이션 (이번 목표 아님)
- EnsembleClassifier 연결 (다음 단계)
- 14건 넘는 대규모 배치 (유형별 1건으로 충분)
- XGBoost 학습 (데이터 축적 단계)
