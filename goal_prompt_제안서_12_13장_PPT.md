# /goal 프롬프트: 12·13장 PPT 슬라이드 제작 (v4 — 전체 편집 가능)

아래 내용을 `/goal` 커맨드의 `$ARGUMENTS`로 사용.

---

## 목표

「교통센터 생성형 AI 상황관제」 사업계획서 원본 PPT를 **복사**한 뒤, **12장(CCTV 영상 기반 교통상황 판단 기능)**과 **13장(영상 기반 생성형 AI 모델 및 검증)** 슬라이드를 기존 양식에 맞게 추가한다.

**v3→v4 변경 사유**: DFD 다이어그램을 이미지(png)로 삽입하면 PPT 내에서 수정 불가. 모든 다이어그램·흐름도·구조도를 **편집 가능한 PPT 도형(shape)**으로 제작한다. 실물 사진(CCTV 캡처, 사고 현장 등)만 이미지로 삽입.

## 산출물

- PPT 파일 1개: `/workspace/prj_cctv/사고분석_설계/docs/제안서_v0.5_12_13장.pptx`
- 빌드 스크립트: `/workspace/prj_cctv/사고분석_설계/docs/build_12_13.py` (덮어쓰기)
- 원본 수정 금지 — 반드시 복사본에 작업

## 입력 자료

### 원본 PPT (복사 대상)

`/공유폴더/파일공유용/JHJ/사고자료/(사업계획서)교통센터 생성형 AI 상환관제_v0.2.pptx`

### 내용 원고

`/workspace/prj_cctv/사고분석_설계/docs/제안서_12_13장_내용정리.md`

### 이미지 자산 (`docs/assets/`) — 실물 사진만 사용

| 파일 | 해상도 | 용도 | 삽입 여부 |
|------|--------|------|-----------|
| `dwelling_grid.jpg` | 4320×960 | CCTV 30프레임 체류캡처 (가로형) | **이미지 삽입** |
| `tracks_visual.png` | 640×1440 | YOLO+ByteTrack 추적 (세로형) | **이미지 삽입** |
| `BIN0004.png` | 197×153 | CCTV 사고촬영 화면 | **이미지 삽입** |
| `BIN0002.png` | 456×343 | 사고 현장 사진 | **이미지 삽입** |
| `outbreak_capture_sample.jpg` | 720×480 | ITS 자동캡처 실물 | **이미지 삽입** |
| `dfd-2.png` | 2660×1500 | 전체 시스템 흐름도 | **도형으로 재현 (이미지 삽입 금지)** |
| `dfd-3.png` | 2660×1500 | AI 판단 호출 흐름 | **도형으로 재현 (이미지 삽입 금지)** |
| `dfd-4.png` | 2660×1500 | 차종분류·보고서·개발계획 | **도형으로 재현 (이미지 삽입 금지)** |

### 기존 빌드 스크립트 (헬퍼 재활용)

`/workspace/prj_cctv/사고분석_설계/docs/build_12_13.py`

재활용할 것: `set_font`, `add_text_box`, `add_rounded_rect`, `add_rect`, `add_arrow_right`, `add_arrow_down`, `add_table`, `setup_slide_header`, `add_image_with_caption`, 색상 팔레트, 위치 상수. **`add_multiline_rounded_rect` 헬퍼도 있으면 재활용.**

## 원본 PPT 디자인 규격

- **슬라이드 크기**: 11.69 × 8.27 inches (A4 가로)
- **레이아웃 4** 사용: "02_과업 추진체계 및 일정의 적정성"
- **공통 요소**: 장번호(금색 #FFC000, 20pt), 대제목(placeholder idx=0), 소제목(placeholder idx=10, 18pt Bold), 설명문(16pt)
- **색상**: #045FA4(주), #0867BE(카테고리), #00B0F0(서브), #184DA4(태그), #FFC000(금색)

### PowerPoint 호환성 필수 규칙

1. **모든 `<a:r>`에 `<a:rPr>` 필수** — `cell.text =` 대신 `tf.clear()` + `p.add_run()` + `run.font.size` 사용
2. **placeholder 대제목**: `ph.text_frame.clear()` + `p.add_run()` + `run.font.language_id = 0x0412`
3. **테이블 헤더 색상**: `tcPr.insert(0, solidFill)` (append가 아닌 insert로 스키마 순서 준수)

## ★ 가시성 규칙 (반드시 준수)

| 항목 | 제한 |
|------|------|
| **최소 폰트** | 10pt (캡션·범례 포함 어디에도 9pt 이하 금지) |
| **본문 텍스트** | 12pt 이상 |
| **도형 라벨** | 11pt 이상 |
| **표 내부** | 10pt 이상 |
| **슬라이드당 도형 수** | **30개 이하** (DFD 도형화로 v3의 25개에서 완화) |
| **이미지 삽입** | 실물 사진(CCTV, 사고현장)만 허용. 다이어그램/흐름도는 도형 |

## ★ DFD 도형화 규칙 — 이미지 사용 절대 금지

DFD 원본 이미지(`dfd-2.png`, `dfd-3.png`, `dfd-4.png`)를 **참고만** 하고, 내용을 **PPT 도형(shape)**으로 재현한다.

### 도형화 방법

- `add_rounded_rect` — 프로세스 블록
- `add_rect` — 데이터/저장소 블록
- `add_arrow_right` / `add_arrow_down` — 흐름 방향
- `add_text_box` — 라벨, 설명
- `add_table` — 비교표, 수치표
- 다이아몬드(판단 분기)는 `MSO_SHAPE.DIAMOND` 사용

### 도형화 원칙

1. **원본 DFD 그대로 재현하지 않는다** — 핵심 흐름만 추출하여 단순화
2. **한 슬라이드에 넣을 수 있는 수준으로 요약** — 도형 30개 이하
3. **모든 텍스트 10pt 이상** — 도형 안 텍스트도 예외 없음
4. **PPT에서 클릭하면 텍스트/위치/색상 수정 가능해야 함**

## DFD 내용 요약 (도형 재현 시 참고)

### dfd-2 (전체 시스템 흐름도) — 12-A 슬라이드에 사용

5단계 가로 파이프라인:

```
입력                → ① 영상 분석         → ② AI 판단 (핵심)      → ③ 데이터 저장     → 출력
─────────────────────────────────────────────────────────────────────────────────────────
· CCTV 영상 (1fps)    · 차량 탐지 (YOLO)     · 장면 파악              · 차량 궤적          · 사고 현황
· 사고 보고서          · 차량 추적 (ByteTrack) · 사고 여부 판정         · 사고 기록          · 차종별 교통량
  (과거 이력)          · 차종 분류 (13종)      · 차종 세분류 보정 (13종) · AI 분석 결과       · 차단 분석
· 지속 데이터          · 속도·충돌위험 계산    · 사고 원인 추론         · 교통 통계          · 보고서 (93컬럼)
  (VMS/VDS)           · 이상 상황 감지        · 보고서 자동 작성 (93컬럼)                   · 위험도 예측
                      · 핵심 장면 선정        · 위험도 평가
                                             ─────────────
                                             Qwen2.5-VL-7B (CPU)
                                             → 72B 모델 (GPU)
                                                                   ④ 사고 예측
                                                                   · 과거 데이터 학습
                                                                   · AI 위험도 활용
                                                                   · 위험 구간 예측
                                                                   · 목표: AUC 0.7+
```

**도형화 지침**: 5개 세로 칼럼(입력/영상분석/AI판단/데이터저장/출력)을 가로로 배치. 각 칼럼은 색상이 다른 둥근사각형. 칼럼 간 화살표 연결. 사고예측은 하단 별도 블록. 각 칼럼 안에 핵심 항목 3~4개를 멀티라인 텍스트로 기재.

### dfd-3 (AI 판단 호출 흐름) — 13-A 슬라이드에 사용

```
이상 감지 조건 (3유형)          AI에 전달하는 정보        판단 흐름
───────────────              ──────────────        ──────────
· 충돌 위험                    · 핵심 장면 1~5장       장면 파악
  - 충돌 예상 3초 미만          · 차량 궤적·속도          ↓
  - 다수 차량 동시 급감속       · 이상 유형             사고인가? (◇)
· 이상 행동                    · IC 위치·차선            ├─ Yes → 차종 세분류 보정 (13종)
  - 급감속·급정차               · 유사 과거 사고                     ↓
  - 역주행·구간 정차                                         원인 분석 + 보고서 작성
· 정거 점검                                                        ↓
  - 5분 주기 자동 촬영                                   분석 결과 (자동 생성)
                                                        - 사고유형·날씨·차종
                                                        - 판단 근거, 보고서 93컬럼
                                                        └─ No → 정상 기록

AI 호출 빈도 표:
| 상황   | 빈도         | CPU 부하 |
| 평시   | 시간당 ~12회 | 10~15%   |
| 혼잡   | 시간당 ~17회 | ~15%     |
| 사고   | 시간당 ~10회 | ~15%     |
```

**도형화 지침**: 좌→우 흐름. 좌측에 3유형 트리거 블록, 중앙에 AI 전달 정보, 다이아몬드(사고인가?), 우측에 결과. 하단에 호출 빈도 표(3행 3열).

### dfd-4 (차종 분류·보고서·개발계획) — 13-C, 13-D 슬라이드에 분산

상단 좌: 차종 13종 매핑
```
보고서 차종 → 13종 세분류
승용차     → T1 승용차
           → T2 버스
버스/승합  → T3~T5 화물 (소·중·대)
화물차     → T6~T7 특수대형
(58%)      → T8~T12 트레일러 (5종)
이륜차     → T13 이륜차
※ 화물차 = 10종 → AI 세분류 필수
```

상단 우: 보고서 93컬럼 자동 작성 비율
```
AI 자작성   27% (25건) ██████████░░░░░░░
AI 추정      8% (7건)  ███░░░░░░░░░░░░░░
현장확인필수 65% (61건) █████████████████
```

하단: 5단계 개발 계획
```
0단계(현재)  → 1단계      → 2단계       → 3단계        → 4단계
데이터수집      영상분석      AI 판단 통합    보고서 자동화     사고 예측
AI 환경 구축    고도화        사고 감지       데이터 축적       모델 개발
AI 사전 학습    목표:12건/s   목표:80%       자동 차종 13/13   AUC 0.7+
```

**도형화 지침**: 13-C에서 보고서 매핑 + 비율 바, 13-D에서 로드맵 5단계 도형.

## 슬라이드 설계 (8장: 12장 4 + 13장 4)

### 12장 (4장)

#### 12-A: 시스템 개요 — 전체 파이프라인 흐름도 (dfd-2 도형화)

- **소제목**: `12.1 현황 문제 및 시스템 개요`
- **설명문**: `사고발생 시 근무자의 주관성 편차를 해소하는 CCTV AI 자동 분석 시스템`
- **구성**:
  - **상단 (70%)**: dfd-2 파이프라인 5단계를 **가로 5칼럼 도형**으로 재현
    - 각 칼럼: 둥근사각형 (너비 ~2.0in × 높이 ~2.8in)
    - 칼럼 색상: 입력(GREEN_OK) / 영상분석(SUB_CAT) / AI판단(MAIN_BG) / 데이터저장(GOLD) / 출력(RED_ALERT)
    - 칼럼 상단: 제목 (14pt Bold White)
    - 칼럼 내부: 핵심 항목 3~4개 (11pt, 멀티라인)
    - 칼럼 간: 오른쪽 화살표
    - AI판단 칼럼 하단: "Qwen2.5-VL (CPU→GPU)" 라벨
  - **하단 (30%)**: 현행 vs 개선 비교 카드 2개
    - 좌: 현행 (RED_ALERT) — "수동 탐색 → 육안 확인 → 구두 보고 | 5~10분" (12pt)
    - 우: 개선 (MAIN_BG) — "자동 연결 → AI 분석 → DB 적재 | ~2분" (12pt)
    - 중앙 화살표
- **목표 도형수**: ~25개

#### 12-B: 3-Tier 감시 체계

- **소제목**: `12.2 전국 CCTV 3-Tier 효율 감시 체계`
- **설명문**: `313대 광역 → 50대 핫스팟 → 동적 정밀, 단계별 자동 승격`
- **구성**:
  - 3-Tier를 가로 3열로 배치
  - 각 Tier: 큰 둥근사각형 (너비 3.3in × 높이 3.0in)
    - Tier명 (14pt Bold White)
    - 대수 (20pt Bold White — 큰 숫자 강조)
    - 기술 1줄 (12pt White)
    - 처리속도 1줄 (12pt 연한파랑)
  - Tier 간 화살표 (승격)
  - 하단: 핵심 수치 카드 3개 — "100% 캡처" / "~2분 소요" / "35컬럼 DB" (12pt)
- **목표 도형수**: ~18개

#### 12-C: CCTV 체류 캡처 실물

- **소제목**: `12.3 사고 현장 CCTV 30초 체류 캡처`
- **설명문**: `ITS 돌발정보 수신 → 인근 CCTV 자동접속 → 1fps × 30초 연속 캡처`
- **구성**:
  - **상단 (60%)**: `dwelling_grid.jpg` 크게 삽입 (너비 10in)
    - 태그: "30초 체류 캡처 실물" (12pt)
    - 캡션: "1fps × 30프레임 — 사고 진행 과정 시계열 기록" (10pt)
  - **하단 좌 (40%)**: `outbreak_capture_sample.jpg` (너비 4.5in)
    - 태그: "ITS 돌발정보 자동 캡처"
  - **하단 우 (40%)**: 타임라인 간소화 3단계
    - "사고접수 → 30초캡처 → AI분석+DB적재" (각 12pt)
    - 총 소요 "약 2분" 강조 (14pt Bold)
- **목표 도형수**: ~15개

#### 12-D: Vision 파이프라인 + 기대효과

- **소제목**: `12.4 AI 비전 파이프라인 및 기대효과`
- **설명문**: `YOLO 차량검출 + ByteTrack 추적 + 7종 이상상황 트리거`
- **구성**:
  - **좌측 (35%)**: `tracks_visual.png` (세로형, 너비 3.5in)
    - 태그: "차량 검출·추적 실물" (12pt)
  - **우측 (65%)**:
    - 상단: 트리거 7종 표 (3열 8행, 10pt)
    - 하단: 기대효과 3항목 카드
      - "객관적 상황 판단" / "24시간 무중단" / "전국 확장성 363대+"
      - 각 14pt Bold 타이틀 + 12pt 설명
- **목표 도형수**: ~18개

### 13장 (4장)

#### 13-A: LLM vs VLM + AI 판단 호출 흐름 (dfd-3 도형화)

- **소제목**: `13.1 텍스트 LLM과 멀티모달 VLM의 상호 보완`
- **설명문**: `Gemma3(텍스트)과 Qwen2.5-VL(영상)이 서로 다른 입력으로 사고 DB를 풍부하게 구축`
- **구성**:
  - **상단 (60%)**: dfd-3 AI 판단 흐름을 도형으로 재현
    - 좌: 이상 감지 3유형 블록 (충돌위험/이상행동/정거점검, 각 둥근사각형)
    - 중앙 상단: "AI 전달 정보" 블록 → "장면 파악" → 다이아몬드 "사고?" → Yes/No 분기
    - 우: "차종 세분류 + 보고서 자동 작성" 결과 블록
    - 각 도형 텍스트 11pt 이상
  - **하단 (40%)**: LLM vs VLM 가로 비교 2열
    - 좌 카드 (CAT_HDR): "Gemma3 (텍스트 LLM)" + 4항목 (12pt)
    - 우 카드 (MAIN_BG): "Qwen2.5-VL (영상 VLM)" + 4항목 (12pt)
    - 하단 중앙: "통합 사고 DB (35컬럼)" 합류 바
    - "경쟁이 아닌 보완" 강조 (14pt Gold)
- **목표 도형수**: ~28개

#### 13-B: MLLM 입출력 실물

- **소제목**: `13.2 멀티모달 AI 분석 입력과 출력`
- **설명문**: `CCTV 영상 5장 입력 → Qwen2.5-VL 추론 → 구조화 JSON 자동 생성`
- **구성**:
  - **좌측 (40%)**: 입력 이미지 세로 2개
    - `BIN0004.png` (3.0in 확대) + 캡션
    - `BIN0002.png` (3.0in) + 캡션
    - 중간 "VLM →" 화살표
  - **우측 (60%)**: AI 분석 결과 카드 (LIGHT_BLUE_BG)
    - "AI 분석 결과" (14pt Bold)
    - 항목 8줄 (12pt) — 기상/차단/차량/적재물/시설물/원인/심각도/신뢰도
    - 하단: "CPU 추론 ~20초/건" (12pt Bold)
- **목표 도형수**: ~18개

#### 13-C: 보고서 매핑 + 차종 13종 (dfd-4 상단 도형화)

- **소제목**: `13.3 도공 보고서 양식 자동 매핑 및 차종 13종 분류`
- **설명문**: `전면차단 보고서 93컬럼 중 AI가 35% 자동 생성 + 화물차 10종 세분류`
- **구성**:
  - **좌측 (50%)**: 보고서 자동 작성 비율
    - 표 또는 도형으로 3행 (AI자작성 27%/AI추정 8%/현장확인 65%)
    - 각 행에 색상 바 (MAIN_BG/YELLOW_TAG/LIGHT_GRAY) + 퍼센트 (12pt Bold)
    - 범례 3색 바 (12pt)
    - "사고 직후 AI가 35% 즉시 채움" 강조 (14pt Bold)
  - **우측 (50%)**: 차종 13종 매핑 도형
    - 좌 칼럼: 보고서 원본 차종 4종 (승용차/버스·승합/화물차/이륜차)
    - 우 칼럼: 13종 세분류 (T1~T13)
    - 화살표 연결
    - "화물차 58% → AI 세분류 필수" 강조 (12pt Bold RED_ALERT)
- **목표 도형수**: ~25개

#### 13-D: 검증 결과 + 개발 로드맵 (dfd-4 하단 도형화)

- **소제목**: `13.4 검증 결과 및 개발 로드맵`
- **설명문**: `실측 성능 수치 + 5단계 개발 계획`
- **구성**:
  - **상단 (35%)**: 검증 수치 카드 5개 (가로)
    - "100%" / "30→5장" / "~20초" / "35컬럼" / "95%"
    - 숫자 24pt Bold White, 라벨 11pt
  - **중단 (30%)**: 핵심 기술 요약 표 (3열 6행)
    - 영상수집/객체검출/이상감지/프리필터/AI분석/DB적재 (10pt)
  - **하단 (35%)**: 5단계 로드맵 도형 (dfd-4 하단 재현)
    - 0단계(현재) → 1단계 → 2단계 → 3단계 → 4단계
    - 각 단계: 제목 (12pt Bold) + 목표 수치 (11pt)
    - 색상: DARK_BG → CAT_HDR → MAIN_BG → SUB_CAT → TAG_LBL
    - 화살표 연결
- **목표 도형수**: ~28개

## 구현 지침

### 필수

1. **원본 복사**: `shutil.copy2()`
2. **기존 build_12_13.py 덮어쓰기** — 헬퍼 함수 재활용
3. **rPr 규칙 준수**: 모든 run에 font 속성 1개 이상 설정
4. **출력 파일명**: `제안서_v0.5_12_13장.pptx`
5. **다이아몬드 도형**: `MSO_SHAPE.DIAMOND` 사용 (dfd-3 분기점)
6. **멀티라인 도형**: 둥근사각형 안에 여러 줄 텍스트를 넣을 때 `\n`으로 분리하고 `tf.word_wrap = True` 설정

### 멀티라인 도형 헬퍼 (필요 시 추가)

도형 하나에 여러 줄(제목+항목 목록)을 넣는 헬퍼. 도형 수를 줄이면서 내용을 담을 수 있다:

```python
def add_multiline_box(slide, left, top, width, height, fill_color,
                      lines, sizes, bolds, colors, alignment=PP_ALIGN.LEFT):
    """하나의 도형에 여러 줄 텍스트 (각 줄 별도 paragraph)"""
    shape = slide.shapes.add_shape(MSO_SHAPE.ROUNDED_RECTANGLE, left, top, width, height)
    if fill_color:
        shape.fill.solid()
        shape.fill.fore_color.rgb = fill_color
    else:
        shape.fill.background()
    shape.line.fill.background()
    tf = shape.text_frame
    tf.word_wrap = True
    for i, (text, size, bold, color) in enumerate(zip(lines, sizes, bolds, colors)):
        if i == 0:
            p = tf.paragraphs[0]
        else:
            p = tf.add_paragraph()
        p.alignment = alignment
        p.space_before = Pt(1)
        p.space_after = Pt(1)
        run = p.add_run()
        run.text = text
        set_font(run, size, bold, color)
    return shape
```

이 헬퍼를 사용하면 dfd-2의 각 칼럼(제목+항목 3~4개)을 **도형 1개**로 표현 가능 → 도형 수 절약.

### 가시성 검증 코드

`main()` 끝에 검증 추가 (이전과 동일): 각 슬라이드에서 10pt 미만 폰트 0개, 도형 30개 이하 확인.

### 하지 말 것

- **DFD 이미지(png) 삽입 절대 금지** — 모든 다이어그램은 편집 가능 도형으로
- 원본 PPT 직접 수정 금지
- 9pt 이하 폰트 금지
- DFD 원본을 그대로 복제하려 하지 말 것 — 핵심만 추출하여 단순화

## 성공 기준

1. 12장 4슬라이드 + 13장 4슬라이드 = 총 **8슬라이드** 추가
2. **DFD 이미지 0개 사용** — 모든 다이어그램이 편집 가능 도형
3. **실물 사진 5개** 사용 (dwelling_grid, tracks_visual, BIN0004, BIN0002, outbreak_capture)
4. 모든 슬라이드에서 **10pt 미만 폰트 0개**
5. 모든 슬라이드에서 **도형 30개 이하**
6. 기존 슬라이드와 디자인 일관성
7. PowerPoint 복구 모드 없이 정상 오픈 (rPr 준수)
8. 원본 PPT 무결
