# 사고분석 파이프라인 연속 개선 루프

## 0. 목표 해부

- **What**: 14건 배치 테스트를 기준으로 파이프라인을 반복 개선하여 판별 정확도를 끌어올린다. 1회 개선 사이클 = 분석 → 가설 → 수정 → 배치 실행 → 검증. 목표 도달 또는 3B 모델 한계 도달까지 반복.
- **Why**: 단일 프롬프트 수정으로는 3B 모델의 판별력 한계를 파악하기 어렵다. 반복 실험으로 어디까지 올릴 수 있는지 경험적으로 확인하고, 병목이 "프롬프트"인지 "모델 능력"인지 규명해야 다음 단계(모델 교체, 학습 등)의 우선순위를 정할 수 있다.
- **Scope**: run_pipeline.py 프롬프트/로직만 수정. 모델 교체·학습·인프라 변경 없음.
- **Final Target**:
  - 판별 정확도 ≥ 11/14 (79%)
  - 정상 ≥ 6/7, 비정상 ≥ 5/7
  - Vehicles 실제값 ≥ 12/14
  - 에러 0건 완주
- **종료 조건**: Target 달성 OR 2회 연속 정확도 변화 없음 (3B 한계 도달)

## 1. 반복 개선 프로토콜

매 사이클마다 아래 5단계를 **엄격히** 수행한다.

### 1-1. 에러 분석 (Analyze)

직전 배치 결과에서 **오답 클립만** 추출하여 원인을 분류한다:

| 오답 유형 | 설명 | 개선 레버 |
|-----------|------|----------|
| FN-시각 | 비정상인데 False — 영상에 이상이 보이나 모델이 미감지 | 프롬프트 관점 추가 |
| FN-미묘 | 비정상인데 False — 영상만으로 판별 불가능 (방향지시등 등) | 멀티프레임 비교 강화 또는 포기 판정 |
| FP-과잉 | 정상인데 True — 정상 교통류를 이상으로 오판 | 프롬프트에 정상 기준 강화 |
| ERR-파싱 | JSON 파싱 실패 → anomaly=None | 프롬프트 간결화, max_tokens 조정 |

### 1-2. 가설 수립 (Hypothesize)

오답 원인에 맞는 수정안을 **1~2개만** 세운다.
- 한번에 너무 많이 바꾸면 어떤 수정이 효과인지 모름
- 가설 예시: "차선 유형(실선/점선) 판별이 약한 이유는 프롬프트에서 차선 관찰을 명시하지 않아서"

### 1-3. 수정 적용 (Modify)

- run_pipeline.py의 SCENE_PROMPT 또는 전후처리 로직만 수정
- 수정 전 코드를 주석 없이 교체 (이전 버전은 git에 남아 있으므로)
- 수정 내용을 아래 실험 로그에 1줄로 기록

### 1-4. 배치 실행 (Execute)

```bash
# DB 초기화
python3 -c "import duckdb; c=duckdb.connect('/DATA/aihub_71566/accident.duckdb'); [c.execute(f'DELETE FROM {t}') for t in ['tracks','mllm_responses','accidents']]; c.close()"

# 14건 배치 실행
cd /workspace/prj_cctv/사고분석_설계/src && python3 test_batch_14.py
```

### 1-5. 검증 + 기록 (Verify)

결과를 아래 형식으로 **실험 로그 테이블에 추가**한다:

## 2. 실험 로그

| Round | 수정 내용 | 정상 | 비정상 | 전체 | 소요 | 에러 |
|-------|----------|------|--------|------|------|------|
| V2 | 한국어, 1장, 비정상 예시만 | 6/7 | 2/7 | 8/14 (57%) | 1336s | 1 |
| V3 | 한국어, 3장, 정상+비정상예시, 싱글턴, max512, 업스케일 | 7/7 | 1/7 | 8/14 (57%) | 1726s | 0 |
| R1 | 영어, 1장, "even a hint→true" 편향 | 1/7 | 7/7 | 8/14 (57%) | 699s | 0 |
| R2 | 영어, 1장, "clearly see→true" | 1/7 | 5/7 | 6/14 (43%) | 705s | 0 |
| **R3** | **영어, 1장, CoT 3단계, "Most is NORMAL" 앵커** | **5/7** | **3/7** | **8/14 (57%)** | **836s** | **0** |

### 결론: 3B 모델 한계 도달

4라운드 실험 결과 정확도 57%에서 고착. 프롬프트 편향 조절은 정상↔비정상 간 트레이드오프만 발생시킴.

**구조적 한계**:
- 3B 모델은 정지 이미지에서 방향지시등 on/off, 실선/점선 구분을 신뢰성 있게 판별하지 못함
- 정상 클립도 차선변경을 포함(합법적)하여 영상만으로 정상/비정상 구분이 본질적으로 어려움
- 최적 균형: R3 (정상 5/7, 비정상 3/7) — CoT + 영어 + 정상 앵커

**다음 단계 제안 (우선순위)**:
1. 모델 업그레이드 (7B+, GPU 필요) — 시각적 디테일 판별력 향상
2. 규칙기반 보완 — L1 트리거(차선 IoU, 방향지시등 detector)와 MLLM 결합
3. 시계열 입력 — 비디오 모델 또는 연속 프레임 차분 분석

## 3. 개선 후보 풀 (우선순위순)

아래에서 매 라운드 1~2개씩 선택한다. 사용한 후보는 체크.

### 프롬프트 엔지니어링
- [ ] 차선 유형(실선/점선) 명시적 판별 지시 추가
- [ ] 방향지시등 on/off 판별 지시 추가
- [ ] 차량 간 거리(안전거리) 판별 지시 추가
- [ ] 비정상 예시를 유형별로 세분화 (7유형 × 1예시)
- [ ] "확실하지 않으면 false" → "의심되면 true" 편향 조정
- [ ] 프롬프트 언어 영어 전환 (3B 모델 영어 성능이 더 높을 수 있음)
- [ ] CoT(Chain-of-Thought) 추가: "먼저 관찰 사항을 나열하고 그 다음 JSON 출력"
- [ ] 프롬프트 토큰 수 축소 (현재 ~400 → 200으로 입력 비용 절감)

### 전후처리 로직
- [ ] 멀티프레임 spread 조정 (5 → 3 또는 7)
- [ ] MLLM 2회 호출 → 다수결 (max_mllm_calls=2, 두 응답 합산)
- [ ] 트리거 프레임 대신 keyframe_selector 활용
- [ ] 저해상도 업스케일 방식 변경 (LANCZOS → CUBIC)
- [ ] 응답 후처리: confidence < 0.5면 anomaly 무시

### 구조 변경 (최후수단)
- [ ] 2-stage 호출: 1차 "장면 설명", 2차 "이상 판별" 분리
- [ ] 클립 내 전체 프레임 그리드 합성 → 1장으로 전달
- [ ] 비정상 유형 힌트를 트리거 정보에 포함 ("이 영상은 차선변경 관련입니다")

## 4. 하지 말 것

- 모델 교체 (3B → 7B)
- 모델 학습/fine-tuning
- 14건 이외 추가 테스트 클립 (유형당 1건 고정)
- DB 스키마 변경
- 한 라운드에 3개 이상 동시 수정
- 이전 라운드 코드를 주석으로 남기기 (git 이력 활용)

## 5. 사이클 실행 규칙

1. **V3 결과 대기**: 현재 실행 중인 V3 배치 완료 후 결과를 실험 로그에 기록
2. **오답 분석**: V3에서 틀린 클립의 MLLM 응답을 DuckDB에서 조회하여 원인 파악
3. **R1 수정**: 가설에 맞는 프롬프트 수정 1~2개 적용
4. **R1 실행**: DB 초기화 → 배치 실행 → 결과 기록
5. **반복**: Target 달성 또는 2회 연속 변화 없음까지

## 6. 산출물

1. 최종 SCENE_PROMPT (가장 높은 정확도 달성 버전)
2. 실험 로그 테이블 (전 라운드 기록)
3. 3B 모델 한계 분석 (어떤 유형이 구조적으로 불가능한지)
4. 다음 단계 제안 (모델 교체? 학습? 규칙기반 보완?)
