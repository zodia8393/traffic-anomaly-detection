# 멀티모달 교통사고 상황 이해 및 예측 시스템

> MLLM(Multi-modal LLM)을 핵심 두뇌로, CCTV 영상에서 사고감지 · 차종분류 · 사고원인 추론 · 자동보고서 · 사고예측을 통합 수행하는 시스템.
> 
> **보고서 DB 원본**: `표준화결과_2024년_1월_1~15일.xlsx` (93컬럼, 24건) — 도공 사고보고서 표준 서식 + 검증로그(규칙추출→LLM보정)

---

## 핵심 사상

**MLLM이 시스템의 중심이다.** Vision Layer는 전처리(검출·추적)로 MLLM의 입력을 준비하고, Metadata Layer는 MLLM의 출력을 축적하며, 사고예측 모델은 MLLM이 만든 구조화 데이터를 소비한다. 모든 고수준 판단(사고 여부, 원인, 심각도, 분류 보정)은 MLLM이 수행한다.

**이미 검증된 패턴**: 보고서 DB의 검증로그 시트가 "규칙 추출 → LLM 보정" 파이프라인을 이미 실증. 본 시스템은 이 패턴을 CCTV 영상으로 확장한 것이다.

```
기존: Vision → 규칙/ML → 판단
전환: Vision → MLLM → 판단        ← MLLM이 판단의 주체
```

---

## 설계 원칙

| # | 원칙 | 근거 |
|---|------|------|
| 1 | **MLLM 중심 아키텍처** | 규칙·ML은 보조, MLLM이 판단 주체 |
| 2 | POC = CPU 로컬 MLLM | GPU 도입 전까지 양자화 모델 사용 |
| 3 | GPU 도입 시 스케일업 | 모델 교체만으로 전환 (인터페이스 고정) |
| 4 | 점진적 확장 | 기존 pipeline/ 보존, new/ 하위 구축 |
| 5 | 현 13종(T1~T13) 유지 | 기존 학습 자산 재활용 |
| 6 | 다수 CCTV 범용 | IC별 자동 적응 |

---

## 보고서 DB 실제 서식 (93컬럼)

`표준화결과_2024년_1월_1~15일.xlsx` 기준. MLLM 자동 보고서 출력이 이 서식을 준수해야 한다.

### 핵심 컬럼 그룹 (8개)

| 그룹 | 컬럼 수 | 주요 컬럼 | MLLM 생성 가능 여부 |
|------|---------|----------|-------------------|
| **시간** | 35 | 접보시각, 순찰발·견인·119·소통재개·완료 시각 | 접보시각만 (나머지는 현장 대응) |
| **위치** | 5 | 노선명, 방향, 이정, 관할지사, 사고지점유형(JC/IC/터널) | CCTV 위치 기반 자동 매핑 |
| **환경** | 1 | 기상 (맑음/흐림/비/눈) | MLLM 장면 판단 또는 외부 API |
| **사고 특성** | 7 | 접보유형, 사고유형(부분/전면차단), 사고원인, 화재, 전복, 적재물 | **MLLM 핵심 추론 대상** |
| **피해 차량** | 10 | 피해차량_1~10 (승용차/화물차/버스/승합차) | **Vision+MLLM 차종분류** |
| **인명 피해** | 4 | 총수, 사망, 중상, 경상 | MLLM 심각도 추정 (확정은 현장) |
| **차로 피해** | 12 | 사고방향/반대방향 1~5차로+갓길 (유/무) | **MLLM 키프레임 분석** |
| **교통 영향** | 7 | 정체길이, 도로피해, 시설물피해 | MLLM + 교통류 데이터 |

### 한국도로공사 13종 차종분류 체계

참조: `/workspace/AI교통량조사/docs/차종분류_참고자료/한국도로공사_교통량조사 차종별 대표 이미지_20250601/`

| T코드 | 차종명 | 단위·축수 | 설명 | 대표 차량 | CCTV 시각 특징 |
|-------|--------|----------|------|----------|---------------|
| **T1** | 승용차·미니트럭 | 1단위 2축 | 소형 차량 전반 | 승용차, 경차, SUV, 1톤미만 트럭 | 짧은 차체, 낮은 높이 |
| **T2** | 버스 | 1단위 2축 | 승객 운송 대형 | 시내버스, 고속버스, 전세버스 | 긴 차체, 높은 높이, 창문 다수 |
| **T3** | 소형 화물차 | 1단위 2축 | 1~2.5톤 미만 | 포터, 봉고 | T1보다 긴 적재함, 낮은 높이 |
| **T4** | 중형 화물차 | 1단위 2축 | 2.5~8.5톤 미만 | 마이티, 메가트럭 | 박스형 적재함, T3보다 큰 차체 |
| **T5** | 대형 화물차 | 1단위 3축 | 8.5톤 이상, 3축 | 대형 카고, 덤프 | 후축 복륜(더블타이어), 긴 차체 |
| **T6** | 대형 특수 | 1단위 4축 | 4축 단일차체 | 대형 덤프, 레미콘 | T5보다 긴 축간거리, 4축 |
| **T7** | 대형 특수 | 1단위 5축 | 5축 단일차체 | 특수 중장비 운반 | 매우 긴 차체, 5축 |
| **T8** | 세미트레일러 | 2단위 4축 | 트랙터+트레일러 | 컨테이너 트레일러(소) | 연결부 꺾임, 2단위 구분 |
| **T9** | 풀트레일러 | 2단위 4축 | 트럭+트레일러 | 풀트레일러(소) | 독립 2차체, 연결봉 |
| **T10** | 세미트레일러 | 2단위 5축 | 트랙터+트레일러 | 40ft 컨테이너, 유조차 | 가장 흔한 대형 트레일러 |
| **T11** | 풀트레일러 | 2단위 5축 | 트럭+트레일러 | 풀트레일러(대) | 독립 2차체, 5축 |
| **T12** | 세미트레일러 | 2단위 6축 | 트랙터+트레일러 | 초대형 화물, 중장비 | 최대 축수, 매우 긴 차체 |
| **T13** | 이륜차 | — | 오토바이, 스쿠터 | 배달 오토바이 | 매우 작은 실루엣, 2륜 |

#### 분류 기준 축 (MLLM 판단 근거)

```
1차: 단위 수 — 1단위(T1~T7) vs 2단위(T8~T12) vs 이륜(T13)
2차: 축 수   — 2축(T1~T4) vs 3축(T5) vs 4축+(T6~T12)
3차: 차체 형태 — 승용(T1) vs 버스(T2) vs 화물(T3~T12)
4차: 크기    — 소형(T3) vs 중형(T4) vs 대형(T5+)

MLLM 핵심 판단 포인트:
  ① 연결부(2단위) 여부 → T8~T12 즉시 분리
  ② 후축 복륜(더블타이어) → T5 이상 확정
  ③ 적재함 유무·크기 → T1 vs T3~T4 구분
  ④ 창문 패턴 → T2(버스) 확정
  ⑤ bbox 종횡비 → 긴 차체(T5+) vs 짧은 차체(T1, T3)
```

#### 사고보고서 → T코드 매핑 (MLLM 보정 필수)

| 보고서 차종 | 직접 매핑 | MLLM 세분류 대상 | 비고 |
|------------|----------|-----------------|------|
| **승용차** | T1 | — | 직접 매핑 (경차, SUV 포함) |
| **버스** | T2 | — | 직접 매핑 |
| **승합차** | T1 or T2 | MLLM 판단 | 승합밴→T1, 소형버스→T2 |
| **화물차** | — | **T3~T12 중 택1** | **보고서 최대 난제: MLLM+Vision 협업 필수** |

> **"화물차" 문제**: 보고서에서 가장 많이 등장하는 차종(24건 중 14건 1순위)이지만, 실제로는 T3(포터)부터 T12(초대형 트레일러)까지 10종에 걸쳐 있음. MLLM이 CCTV 키프레임에서 차축 수, 연결부, 차체 크기를 판단하여 세분류해야 하는 핵심 태스크.

### 검증로그 시트 — 기존 파이프라인 참조

```
규칙 추출: 원문 PDF → 정규식/규칙 → JSON (93 필드)
LLM 보정: 규칙 추출 결과 → LLM → 보정 (오류 수정, 누락 보완)
```

이 "규칙→LLM" 2단계 패턴이 본 시스템의 "Vision→MLLM" 구조와 동일.

---

## 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                            INPUT                                     │
│  ┌──────────────┐  ┌──────────────────┐  ┌───────────────────────┐  │
│  │ CCTV 영상     │  │ 사고 보고서 DB   │  │ 차축정보 메타데이터   │  │
│  │              │  │ (93컬럼 표준서식) │  │ (WIM / VDS / TCS)    │  │
│  └──────┬───────┘  └────────┬─────────┘  └──────────┬────────────┘  │
└─────────┼──────────────────┼───────────────────────┼────────────────┘
          │                  │                       │
          ▼                  │                       │
┌─────────────────────────┐  │                       │
│  Layer 1: Vision         │  │                       │
│  (전처리 — MLLM 입력준비) │  │                       │
│                          │  │                       │
│  YOLO 객체 탐지          │  │                       │
│  ByteTrack 추적          │  │                       │
│  속도·거리·TTC 계산      │  │                       │
│  이상 트리거 감지        │  │                       │
│  키프레임 선정           │  │                       │
│  차종분류 (13종, 1차)    │  │                       │
└────┬────────────┬───────┘  │                       │
     │            │          │                       │
     │ 키프레임   │ 구조화    │                       │
     │ (이미지)   │ 메타데이터│                       │
     │            │          │                       │
     ▼            ▼          ▼                       ▼
┌────────────────────────────────────────────────────────────────────┐
│                                                                     │
│              ★ Layer 3: MLLM (핵심 두뇌) ★                         │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    멀티모달 입력                              │   │
│  │  ┌────────────┐  ┌─────────────────┐  ┌─────────────────┐  │   │
│  │  │ 키프레임    │  │ 차량 궤적·속도   │  │ 과거 사고 보고서 │  │   │
│  │  │ (1~5장)    │  │ TTC·행동패턴    │  │ (유사 사례)     │  │   │
│  │  └────────────┘  └─────────────────┘  └─────────────────┘  │   │
│  └─────────────────────────┬───────────────────────────────────┘   │
│                            │                                        │
│                            ▼                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    MLLM 수행 태스크                           │   │
│  │                                                              │   │
│  │  1. 장면 이해       "현재 교통 상황을 설명하시오"              │   │
│  │  2. 사고 감지       "사고가 발생했는가? 유형은?"               │   │
│  │  3. 차종 분류 보정  "Vision 1차 분류 T4를 검증하시오"          │   │
│  │  4. 사고 원인 추론  "사고 원인과 기여 요인을 분석하시오"        │   │
│  │  5. 자동 보고서     "사고 보고서를 서식에 맞게 작성하시오"      │   │
│  │  6. 위험도 평가     "현재 구간의 사고 위험도를 평가하시오"      │   │
│  └─────────────────────────┬───────────────────────────────────┘   │
│                            │                                        │
│  ┌─────────────────────────▼───────────────────────────────────┐   │
│  │                    구조화 출력 (JSON)                         │   │
│  │                                                              │   │
│  │  { "accident_detected": true,                                │   │
│  │    "accident_type": "rear_end_collision",                    │   │
│  │    "severity": "moderate",                                   │   │
│  │    "cause": "급감속 선행차 + 후행차 안전거리 미확보",          │   │
│  │    "vehicle_corrections": {"track_42": "T4→T5"},             │   │
│  │    "risk_score": 0.82,                                       │   │
│  │    "report": "..." }                                         │   │
│  └─────────────────────────┬───────────────────────────────────┘   │
│                            │                                        │
└────────────────────────────┼────────────────────────────────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────────┐  ┌────────────────────┐
│ Layer 2:      │  │ 사고 알림 / 보고 │  │ Layer 4: 사고예측   │
│ Metadata DB   │  │ (즉시 출력)      │  │ (축적 데이터 기반)  │
│               │  │                  │  │                    │
│ MLLM 출력 저장│  │ 사고 감지 알림   │  │ 위험도 시계열 학습  │
│ 시계열 집계   │  │ 자동 보고서      │  │ 사고 확률 예측     │
│ 이력 관리     │  │ 원인 분석 리포트 │  │ 위험 구간 순위     │
└──────────────┘  └──────────────────┘  └────────────────────┘
```

---

## MLLM 전략

### POC (현재 — CPU 로컬)

| 항목 | 내용 |
|------|------|
| **모델** | Qwen2.5-VL-7B-Instruct |
| **양자화** | Q4_K_M (GGUF, llama.cpp) |
| **메모리** | ~6GB VRAM → CPU RAM 사용 (122GB 중 ~8GB) |
| **추론 속도** | ~20~40초/요청 (이미지 1~3장 + 텍스트) |
| **런타임** | llama-cpp-python (OpenAI 호환 API 서버) |
| **운용 방식** | 트리거 기반 배치 (매 프레임 호출 아님) |

#### POC MLLM 후보 비교

| 모델 | 파라미터 | Q4 크기 | 한국어 | 벤치마크 (MMBench) | 비고 |
|------|---------|---------|--------|-------------------|------|
| **Qwen2.5-VL-7B** | 7.6B | ~4.5GB | O (학습 포함) | 82.6 | 최우선 후보 |
| InternVL2-8B | 8B | ~5GB | △ | 81.7 | 대안 |
| Phi-3.5-Vision | 4.2B | ~2.8GB | △ | 69.2 | 경량 대안 |
| MiniCPM-V-2.6 | 8B | ~5GB | O | 78.0 | 중국어/한국어 강점 |

**1차 선정: Qwen2.5-VL-7B** — 벤치마크 최고 + 한국어 지원 + 양자화 안정

### GPU 도입 후 (향후)

| 항목 | 내용 |
|------|------|
| **모델** | Qwen2.5-VL-72B 또는 차기 최신 MLLM |
| **추론** | vLLM / TGI (GPU 서빙) |
| **속도** | ~1~3초/요청 |
| **전환 방법** | `MLLMClient` 엔드포인트 URL만 변경 (인터페이스 동일) |

### MLLM 호출 인터페이스 (GPU 전환 대비 추상화)

```python
class MLLMClient:
    """MLLM 추상 인터페이스 — 백엔드 교체 시 이것만 변경."""

    def __init__(self, backend: str = "llama_cpp"):
        # POC:  backend="llama_cpp"  → 로컬 CPU
        # GPU:  backend="openai_api" → vLLM/TGI 서버
        ...

    def analyze_scene(
        self,
        key_frames: list[np.ndarray],     # 키프레임 이미지 (1~5장)
        metadata: dict,                     # Vision Layer 구조화 데이터
        task: str,                          # "detect" | "classify" | "cause" | "report" | "risk"
        context: str | None = None,         # 과거 사고 보고서 등 추가 컨텍스트
    ) -> dict:
        """멀티모달 입력 → 구조화 JSON 출력."""
        ...
```

---

## MLLM 트리거 정책 (효율적 호출)

MLLM은 매 프레임 호출하지 않는다. Vision Layer가 이상 징후를 감지했을 때만 MLLM을 호출한다.

```
트리거 조건 (하나라도 충족 시 MLLM 호출):
  ┌─────────────────────────────────────────────────┐
  │ T1. TTC < 3.0초 (충돌 임박)                      │
  │ T2. 급감속 감지 (가속도 < -3m/s², 2프레임 이상)   │
  │ T3. 정차 차량 (비정차 구간에서 속도 < 5km/h, 3초) │
  │ T4. 역주행 감지 (반대 벡터, 3프레임 지속)         │
  │ T5. 다중 차량 급감속 (3대+, 2초 이내)             │
  │ T6. 속도 분산 급증 (σ > 2σ_baseline)             │
  │ T7. 주기 호출 (5분 간격, 정상 상황 요약)          │
  └─────────────────────────────────────────────────┘

트리거 시 MLLM에 전달:
  - 트리거 시점 키프레임 1~5장 (crop_quality_score 상위)
  - 트리거 전후 10초 차량 궤적·속도·TTC 시계열
  - 트리거 유형 코드 (T1~T7)
  - IC 정보 (이름, 차선수, 구간 특성)
```

### 호출 빈도 추정 (POC)

| 상황 | 트리거 빈도 | MLLM 호출 | CPU 부하 |
|------|-----------|-----------|----------|
| 정상 교통류 (1시간) | T7 × 12회 | 12회 × ~30초 = 6분 | 10% |
| 혼잡 구간 (1시간) | T1~T6 × ~5회 + T7 × 12 | ~17회 × ~30초 = 8.5분 | 14% |
| 사고 발생 (이벤트) | T1~T5 집중 × ~10회 | ~10회 × ~30초 = 5분 | 집중 처리 |

**결론**: CPU에서도 트리거 기반이면 실시간 대비 충분한 여유. 영상 1시간 처리에 MLLM 부하 10~15%.

---

## MLLM 프롬프트 설계 (태스크별)

### Task 1: 장면 이해

```
당신은 교통 CCTV 영상 분석 전문가입니다.

[이미지: 키프레임 1~3장]

아래는 현재 CCTV 영상에서 추출한 교통 상황 데이터입니다:
- 위치: {ic_name} ({lane_count}차로, {road_type})
- 검출 차량: {vehicle_count}대
- 차종 분포: {class_distribution}
- 평균 속도: {avg_speed} km/h
- 트리거: {trigger_type} — {trigger_description}

이 장면의 교통 상황을 분석하고 JSON으로 응답하시오:
{
  "scene_description": "장면 설명",
  "traffic_state": "free_flow | congested | incident | stopped",
  "anomalies": ["이상 상황 목록"],
  "risk_level": "low | medium | high | critical"
}
```

### Task 2: 사고 감지

```
당신은 교통사고 감지 전문가입니다.

[이미지: 트리거 시점 키프레임 3~5장 (시간순)]

교통 상황 데이터:
- 트리거: {trigger_type}
- 관련 차량 궤적:
  {track_summaries}
- TTC: {ttc_values}
- 속도 변화: {speed_changes}

사고 발생 여부를 판단하고 JSON으로 응답하시오:
{
  "accident_detected": true/false,
  "confidence": 0.0~1.0,
  "accident_type": "rear_end | sideswipe | rollover | head_on | fixed_object | none",
  "severity": "minor | moderate | severe | fatal",
  "involved_vehicles": [{"track_id": N, "role": "striking|struck|..."}],
  "timestamp_estimated": "HH:MM:SS",
  "reasoning": "판단 근거 설명"
}
```

### Task 3: 차종 분류 보정 (한국도로공사 13종 기준)

```
당신은 한국도로공사 교통량조사 차종분류 전문가입니다.

[이미지: 해당 차량 crop (best quality)]

Vision 모델의 1차 분류 결과: {vision_class} (신뢰도: {confidence})
차량 메타데이터: bbox 면적 {area}px², 종횡비 {aspect_ratio}

한국도로공사 13종 분류 체계 (차축 기반):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1단위(단일차체):
  T1  = 승용차·미니트럭 (2축, 소형)
  T2  = 버스 (2축, 창문 다수)
  T3  = 소형화물 1~2.5t 미만 (2축, 포터/봉고급)
  T4  = 중형화물 2.5~8.5t 미만 (2축, 마이티급)
  T5  = 대형화물 8.5t+ (3축, 후축 복륜)
  T6  = 대형특수 (4축, 레미콘/대형덤프)
  T7  = 대형특수 (5축)

2단위(트랙터+트레일러 또는 트럭+트레일러):
  T8  = 세미트레일러 (4축)
  T9  = 풀트레일러 (4축)
  T10 = 세미트레일러 (5축, 40ft 컨테이너)
  T11 = 풀트레일러 (5축)
  T12 = 세미트레일러 (6축, 초대형)

이륜:
  T13 = 이륜차 (오토바이, 스쿠터)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

판단 순서:
1) 2단위(연결부 꺾임) 여부 → T8~T12
2) 이륜(2륜, 매우 작음) → T13
3) 창문 패턴(승객용) → T2(버스)
4) 적재함 유무·축 수로 T1/T3~T7 구분

JSON으로 응답:
{
  "corrected_class": "T코드",
  "confidence": 0.0~1.0,
  "unit_count": 1 | 2,
  "axle_count": 2~6,
  "vision_agreed": true/false,
  "reasoning": "판단 근거 (차축, 차체형태, 크기 기반)"
}
```

### Task 4: 사고 원인 추론 + 보고서 (도공 93컬럼 표준 서식 준수)

```
당신은 교통사고 분석 전문가입니다. 한국도로공사 사고보고서 표준 서식에 맞춰 분석합니다.

[이미지: 사고 전후 키프레임 5장 (시간순)]

사고 상황 데이터:
- CCTV 위치: {ic_name} ({road_name}, {direction}방향, 이정 {km}km)
- 사고 유형: {accident_type}
- 관련 차량:
  {involved_vehicles_detail}
- 사고 전 30초 교통류:
  {pre_accident_traffic}
- 기상: {weather}
- 유사 과거 사고 사례:
  {similar_cases}

도로공사 사고보고서 표준 서식(93컬럼)에 맞춰 JSON으로 작성하시오.
CCTV 영상에서 판단 가능한 항목만 채우고, 현장 확인 필요 항목은 null로 표기:

{
  "시간": {
    "사고접보 시각(날짜)": "YYYY. M. D",
    "사고접보 시각(요일)": "월~일",
    "사고접보 시각(시간)": "HH:MM",
    "안전순찰발 도착시각": null,
    "119 도착시각": null,
    "사고처리 완료시간": null
  },
  "위치": {
    "노선명": "{road_name}",
    "방향": "{direction}",
    "이정": {km},
    "사고 관할 지사": "{branch}",
    "사고지점 유형": "JC | IC | 터널 | 본선 | 진출입"
  },
  "사고 특성": {
    "기상": "맑음 | 흐림 | 비 | 눈 | 안개",
    "접보 유형": "CCTV",
    "사고 유형": "부분차단 | 전면차단 | 갓길",
    "사고원인": "주시태만 | 졸음운전 | 과속 | 안전거리 미확보 | 차로변경 | 기타",
    "화재 여부": "미발생 | 화재",
    "차량 전복/전도 여부": "미 전복/전도 | 전복 | 전도",
    "적재물 유출 여부": "미 유출 | 유출",
    "적재물 유형_1": null
  },
  "피해차량": {
    "피해차량_1": "승용차 | 화물차 | 버스 | 승합차",
    "피해차량_2": "...",
    "피해차량_N": "... (최대 10대)"
  },
  "인명피해": {
    "인명피해(총수)": null,
    "사망자수": null,
    "중상자수": null,
    "경상자수": null
  },
  "차로 피해": {
    "일방향 여부": "일방 | 양방",
    "사고방향 피해 차로_1": "유 | 무",
    "사고방향 피해 차로_2": "유 | 무",
    "사고방향 피해 차로_3": "유 | 무",
    "사고방향 피해 차로_갓길": "유 | 무"
  },
  "교통 영향": {
    "정체길이": "km (정수)",
    "도로피해_1": "차로명",
    "시설물 피해_1": null
  },
  "추론": {
    "cause_primary": "주 원인",
    "cause_contributing": ["기여 요인 목록"],
    "narrative": "사고 경위 서술 (3~5문장)",
    "recommendations": ["재발 방지 권고사항"],
    "confidence": 0.0~1.0,
    "reasoning": "MLLM 판단 근거"
  }
}
```

#### MLLM 생성 가능 vs 현장 확인 필요

| 구분 | 항목 | 비율 |
|------|------|------|
| **MLLM 자동 생성** | 접보시각, 위치(5), 기상, 접보유형, 사고유형, 사고원인, 피해차량, 차로피해, 정체길이 | ~25/93 (27%) |
| **MLLM 추정 (참고용)** | 인명피해, 심각도, 화재/전복/적재물 | ~7/93 (8%) |
| **현장 확인 필수 (null)** | 도착·견인·소통재개 시각, 도로/시설물 피해 상세, 사고처리 완료 | ~61/93 (65%) |

→ MLLM은 사고 즉시 27%를 자동 채우고, 현장 대응자가 나머지를 보완하는 구조.

---

## Layer별 상세

### Layer 1: Vision (전처리 — MLLM 입력 준비)

기존 파이프라인 모듈을 재사용하되, MLLM 입력 준비에 초점.

| 기능 | 기존 모듈 | 변경 |
|------|----------|------|
| 객체 탐지 | detector.py (YOLO11n) | 그대로 |
| 추적 | tracker.py (ByteTrack) | 속도·가속도 출력 추가 |
| 1차 차종분류 | ensemble_classifier.py (Triple V1) | MLLM 보정 전 1차 결과 |
| Virtual Line 카운팅 | video_consistency.py | 그대로 |
| **신규: 속도 추정** | speed_estimator.py | 호모그래피 기반 |
| **신규: TTC 계산** | ttc_calculator.py | bbox 간격 변화율 |
| **신규: 이상 트리거** | trigger_detector.py | MLLM 호출 조건 판정 |
| **신규: 키프레임 선정** | keyframe_selector.py | crop_quality_score 기반 |

#### 키프레임 선정 전략

```python
def select_keyframes(track_history: list[dict], trigger_frame: int,
                     window: int = 150, max_frames: int = 5) -> list[int]:
    """트리거 전후 window 프레임에서 품질 상위 max_frames 선정.

    선정 기준:
    1. crop_quality_score 상위 (bbox 크기 × 검출 신뢰도 × 중앙 근접)
    2. 시간 분산 (연속 프레임 중복 방지, 최소 1초 간격)
    3. 트리거 시점 프레임 필수 포함
    """
    ...
```

### Layer 2: Metadata Store (DuckDB)

MLLM 출력을 포함한 모든 데이터를 구조화 저장.

```sql
CREATE TABLE tracks (
    video_id    VARCHAR,
    track_id    INTEGER,
    ic_name     VARCHAR,
    start_time  TIMESTAMP,
    end_time    TIMESTAMP,
    vehicle_cls_vision VARCHAR,     -- Vision 1차 분류
    vehicle_cls_mllm   VARCHAR,     -- MLLM 보정 분류 (있을 경우)
    vehicle_cls_final  VARCHAR,     -- 최종 확정
    confidence  FLOAT,
    avg_speed   FLOAT,
    trajectory  FLOAT[][],
    PRIMARY KEY (video_id, track_id)
);

CREATE TABLE mllm_responses (
    response_id VARCHAR PRIMARY KEY,
    video_id    VARCHAR,
    trigger_type VARCHAR,           -- T1~T7
    trigger_frame INTEGER,
    task         VARCHAR,           -- detect | classify | cause | report | risk
    input_summary TEXT,
    output_json  JSON,
    latency_sec  FLOAT,
    model_id     VARCHAR,           -- qwen2.5-vl-7b-q4 등
    created_at   TIMESTAMP
);

-- 사고 이벤트 (도공 93컬럼 서식 핵심 필드 + MLLM 추적)
CREATE TABLE accidents (
    event_id    VARCHAR PRIMARY KEY,
    video_id    VARCHAR,
    -- 도공 서식: 위치
    road_name   VARCHAR,            -- 노선명
    direction   VARCHAR,            -- 방향
    km_post     FLOAT,              -- 이정
    branch      VARCHAR,            -- 관할 지사
    point_type  VARCHAR,            -- 사고지점 유형 (JC/IC/터널/본선)
    -- 도공 서식: 시간
    report_time TIMESTAMP,          -- 접보 시각
    -- 도공 서식: 사고 특성
    weather     VARCHAR,            -- 기상
    report_source VARCHAR,          -- 접보 유형 (CCTV/순찰대/고객)
    accident_type VARCHAR,          -- 부분차단/전면차단/갓길
    cause       VARCHAR,            -- 사고원인
    fire        BOOLEAN,
    rollover    BOOLEAN,
    spill       BOOLEAN,
    spill_type  VARCHAR,
    -- 도공 서식: 피해
    vehicles    JSON,               -- [{순번, 차종(보고서), T코드, role}]
    casualties  JSON,               -- {총수, 사망, 중상, 경상}
    lane_damage JSON,               -- {방향: {1차로: 유/무, ...}}
    congestion_km INTEGER,          -- 정체길이
    -- MLLM 추적
    severity    VARCHAR,            -- MLLM 판단 심각도
    mllm_response_id VARCHAR,
    mllm_report_json JSON,          -- 93컬럼 중 자동 생성분 전체
    report_path VARCHAR,            -- 최종 보고서 파일 경로
    FOREIGN KEY (mllm_response_id) REFERENCES mllm_responses(response_id)
);

-- 과거 사고 보고서 아카이브 (유사 사례 검색용)
CREATE TABLE report_archive (
    report_id    VARCHAR PRIMARY KEY,
    source       VARCHAR,           -- TAAS, 도공, 자체
    date         DATE,
    road_name    VARCHAR,
    direction    VARCHAR,
    km_post      FLOAT,
    point_type   VARCHAR,
    weather      VARCHAR,
    accident_type VARCHAR,
    cause        VARCHAR,
    vehicles_summary VARCHAR,       -- "화물차+승용차+화물차"
    casualties_total INTEGER,
    fatalities   INTEGER,
    congestion_km INTEGER,
    full_record  JSON,              -- 93컬럼 전체 (검색·참조용)
    embedding    FLOAT[384],        -- sentence-transformers 임베딩
    created_at   TIMESTAMP
);

CREATE TABLE traffic_agg (
    ic_name      VARCHAR,
    period_start TIMESTAMP,
    volume       INTEGER,
    avg_speed    FLOAT,
    speed_std    FLOAT,
    truck_ratio  FLOAT,
    risk_score   FLOAT,            -- MLLM 위험도 평가
    mllm_scene   VARCHAR,          -- free_flow | congested | incident
    PRIMARY KEY (ic_name, period_start)
);
```

### Layer 4: 사고예측 모델

MLLM이 생성한 구조화 데이터를 피처로 활용하는 전통 ML 모델.

| 피처 유형 | 출처 | 예시 |
|-----------|------|------|
| 교통류 지표 | Vision Layer | 교통량, 속도, 밀도, 점유율 |
| 이상 행동 빈도 | Vision Layer | 급감속·차선변경·정차 횟수 |
| **MLLM 위험도** | MLLM | risk_score (0~1), scene_state |
| **MLLM 이상 감지** | MLLM | anomaly 누적 카운트 |
| 시공간 피처 | 메타데이터 | IC 유형, 시간대, 요일, 과거 사고 이력 |

**모델**: XGBoost (CPU, 해석 가능, MLLM 위험도를 피처로 활용)

---

## 데이터 흐름 (전체)

```
CCTV 영상 입력
    │
    ▼
[1] Vision Layer ─── 매 프레임 처리 (CPU ~3.5ms/frame)
    │  YOLO 검출 → ByteTrack 추적 → 1차 차종분류
    │  속도·TTC 계산 → 트리거 조건 체크
    │
    ├── 정상 흐름 ─────→ [2] Metadata 저장 (교통류 집계)
    │                          │
    │                          ▼ (5분 주기)
    │                    [3] MLLM Task 1: 장면 이해
    │                          │
    │                          ▼
    │                    [4] 위험도 평가 → 사고예측 피처
    │
    └── 트리거 발생 ───→ [3] MLLM 긴급 호출 (~30초 CPU)
                              │
                              ├── Task 2: 사고 감지
                              │     ├── 미감지 → 로그 기록
                              │     └── 감지 → Task 4: 원인 추론 + 보고서
                              │
                              └── Task 3: 차종 보정 (저신뢰 분류 시)
```

---

## 실행 로드맵 (5 Phase)

### Phase 0: 데이터 수집 + MLLM 환경 구축 + 보고서 DB 적재

| 항목 | 내용 |
|------|------|
| 사고 영상 수집 | TAAS, 도공 CCTV, AI Hub 교통사고, 공개 블랙박스 |
| 수집 목표 | 최소 50건 (추돌, 전복, 접촉, 역주행) |
| **보고서 DB 적재** | `표준화결과_*.xlsx` → `report_archive` 테이블 적재 + 임베딩 생성 |
| MLLM 환경 | llama.cpp 빌드 + Qwen2.5-VL-7B Q4 다운로드 + 서빙 검증 |
| 기초 벤치마크 | 사고 영상 10건으로 MLLM 장면 이해 품질 확인 |
| **보고서 생성 검증** | 기존 24건 사고에 대해 MLLM 보고서 생성 → 실제 서식과 비교 |
| **게이트** | 사고 영상 50건 + MLLM 서빙 + 보고서 DB 적재 + 장면 이해 정성 통과 |

### Phase 1: Vision Layer 확장

| 항목 | 내용 |
|------|------|
| 구현 | speed_estimator, ttc_calculator, trigger_detector, keyframe_selector |
| 검증 | 기존 경동 영상으로 속도·TTC 출력 확인 |
| 통합 | vision_pipeline.py → MLLM 입력 포맷 자동 생성 |
| **게이트** | 정상 교통류 MOTA >= 0.5 + 속도 오차 ±15% |

### Phase 2: MLLM 통합 + 사고감지

| 항목 | 내용 |
|------|------|
| 구현 | MLLMClient + 프롬프트 4종 + 트리거-MLLM 연동 |
| 검증 | 수집 사고 영상 50건 → 사고감지 정확도 측정 |
| 보정 | 프롬프트 반복 개선 (few-shot 예시 추가) |
| **게이트** | 사고감지 정확도 >= 80% (Precision + Recall 조화) |

### Phase 3: 자동 보고서 (도공 93컬럼 서식) + Metadata 축적

| 항목 | 내용 |
|------|------|
| 구현 | DuckDB 스키마 + 93컬럼 보고서 생성 + 유사 사례 검색 |
| 보고서 검증 | MLLM 자동 생성분 27% (25/93컬럼) 정확도 측정 |
| 유사 사례 | report_archive 임베딩 → 코사인 유사도 Top-3 검색 |
| 서식 준수 | 실제 `표준화결과_*.xlsx`와 필드명·값 범위 일치 확인 |
| **게이트** | 자동 생성 필드 정확도 >= 85% + 보고서 품질 >= 3.5/5 + DB 역산 통과 |

### Phase 4: 사고예측 모델

| 항목 | 내용 |
|------|------|
| 구현 | feature_engineer + XGBoost 학습 + risk_scorer |
| 학습 데이터 | Phase 2~3에서 축적된 MLLM 판단 + 교통류 시계열 |
| **게이트** | AUC >= 0.7 |

---

## 디렉토리 구조

```
/workspace/prj_cctv/
├── pipeline/                       # 기존 (변경 없음, import만)
│   ├── src/
│   │   ├── detector.py             ← Layer 1 재사용
│   │   ├── tracker.py              ← Layer 1 재사용
│   │   ├── ensemble_classifier.py  ← Layer 1 재사용
│   │   ├── video_consistency.py    ← Layer 1 참조
│   │   └── config.py               ← 공통 참조
│   └── data/models/                ← 검출 모델
│
├── new/                            # 신규 시스템
│   ├── new.md                      ← 이 문서
│   ├── src/
│   │   ├── config_new.py           # 새 시스템 설정
│   │   │
│   │   ├── # --- Layer 1: Vision (전처리) ---
│   │   ├── speed_estimator.py      # 호모그래피 속도 추정
│   │   ├── ttc_calculator.py       # TTC 계산
│   │   ├── trigger_detector.py     # MLLM 트리거 조건 (7종)
│   │   ├── keyframe_selector.py    # 키프레임 선정
│   │   ├── vision_pipeline.py      # Layer 1 통합 실행기
│   │   │
│   │   ├── # --- Layer 2: Metadata ---
│   │   ├── db_schema.py            # DuckDB 스키마
│   │   ├── metadata_writer.py      # Vision+MLLM → DB 저장
│   │   ├── traffic_aggregator.py   # 5분 집계
│   │   ├── report_indexer.py       # 보고서 임베딩/검색
│   │   │
│   │   ├── # --- Layer 3: MLLM (핵심) ---
│   │   ├── mllm_client.py          # MLLM 추상 인터페이스
│   │   ├── mllm_server.py          # llama.cpp 서버 래퍼
│   │   ├── prompts.py              # 태스크별 프롬프트 템플릿
│   │   ├── scene_analyzer.py       # Task 1: 장면 이해
│   │   ├── accident_detector.py    # Task 2: 사고 감지
│   │   ├── class_corrector.py      # Task 3: 차종 보정
│   │   ├── report_generator.py     # Task 4: 원인 추론 + 보고서
│   │   │
│   │   ├── # --- Layer 4: 사고예측 ---
│   │   ├── feature_engineer.py     # 예측 피처 (MLLM 출력 포함)
│   │   ├── accident_predictor.py   # XGBoost 모델
│   │   └── risk_scorer.py          # 위험도 순위화
│   │
│   ├── data/
│   │   ├── accident_videos/        # 사고 영상
│   │   ├── accident_labels/        # 사고 라벨
│   │   ├── reports_db/             # 보고서 DB
│   │   ├── calibrations/           # IC별 캘리브
│   │   └── accident.duckdb         # 메타데이터 DB
│   │
│   ├── models/
│   │   ├── mllm/                   # MLLM 가중치 (GGUF)
│   │   └── predictor/              # XGBoost
│   │
│   └── output/
│       ├── reports/                # 자동 보고서
│       ├── predictions/            # 예측 결과
│       └── evaluations/            # 성능 평가
│
└── output/                         # 기존 (보존)
    └── _models_preserved/
```

---

## 기술 스택

| 계층 | 기술 | 비고 |
|------|------|------|
| 검출 | YOLO11n (Ultralytics) | 기존 재사용, CPU |
| 추적 | ByteTrack (supervision) | 기존 재사용 |
| 1차 분류 | Triple Ensemble V1 | 기존 재사용 |
| 속도 추정 | OpenCV (호모그래피) | 신규 |
| **MLLM (POC)** | **Qwen2.5-VL-7B Q4 + llama.cpp** | **핵심, CPU** |
| **MLLM (GPU 후)** | **Qwen2.5-VL-72B + vLLM** | **엔드포인트만 교체** |
| 메타데이터 | DuckDB | 서버리스 |
| 임베딩 | all-MiniLM-L6-v2 | 보고서 검색, CPU |
| 사고예측 | XGBoost | CPU |
| 시각화 | Streamlit | 대시보드 |

---

## 기존 자산 활용

### 재활용 모듈 (pipeline/ → import)

| 모듈 | 역할 | 변경 |
|------|------|------|
| detector.py | YOLO11n 차량 검출 | 그대로 |
| tracker.py | ByteTrack 추적 | 속도·가속도 출력 추가 |
| ensemble_classifier.py | Triple Ensemble V1 (13종) | MLLM 전 1차 분류 |
| video_consistency.py | Virtual Line + Lazy Cls | 참조 |
| config.py | 경로·상수 | 새 상수 추가 |

### 재활용 모델 가중치

| 파일 | 용도 |
|------|------|
| round_12_v8lb_best.pt | Triple Ensemble 백본 1 |
| round_14_mnv4_unified.pt | Triple Ensemble 백본 2 |
| round_8_mnv4_best.pt | Triple Ensemble 백본 3 |
| yolo11n.pt | 차량 검출기 |

---

## GPU 전환 계획

```
현재 (POC)                         GPU 도입 후
─────────────                     ─────────────
Qwen2.5-VL-7B Q4                  Qwen2.5-VL-72B (FP16)
llama-cpp-python                   vLLM / TGI
~30초/요청                         ~2초/요청
트리거 기반 배치                    준실시간 가능
CPU RAM ~8GB                       GPU VRAM ~40GB

전환 작업:
1. mllm_client.py의 backend="openai_api" + URL 변경
2. 프롬프트 동일 (모델 크기만 다름)
3. 트리거 임계값 낮춤 (속도 여유로 더 자주 호출 가능)
4. 배치 → 스트리밍 전환 (선택)
```

---

## 리스크

| # | 리스크 | 영향 | 대응 |
|---|--------|------|------|
| 1 | 사고 영상 수집 부족 | 전 Phase 차단 | 다수 소스 병행, 최소 50건 |
| 2 | MLLM CPU 추론 품질 미달 | 7B Q4 한계 | few-shot 프롬프트 강화, 모델 교체 후보 대기 |
| 3 | MLLM 환각 (hallucination) | 오탐·허위 보고서 | 구조화 JSON 출력 강제 + Vision 교차 검증 |
| 4 | CPU 속도 추정 부정확 | TTC 신뢰도 하락 | 캘리브 정밀도 우선 확보 |
| 5 | 사고 데이터 희소 | 예측 모델 학습 부족 | MLLM 비지도 장면 이해로 보완 |
| 6 | llama.cpp VLM 지원 불안정 | 서빙 실패 | MiniCPM-V 등 대안 모델 |

---

## 게이트 체계

| Phase | 게이트 조건 | 판정 |
|-------|------------|------|
| 0 | 사고 영상 50건 + MLLM 서빙 동작 + 장면 이해 정성 통과 | PASS / BLOCKED |
| 1 | MOTA >= 0.5 + 속도 오차 ±15% | PASS / BLOCKED |
| 2 | 사고감지 정확도 >= 80% | PASS / BLOCKED |
| 3 | 보고서 품질 >= 3.5/5 + DB 역산 통과 | PASS / BLOCKED |
| 4 | 사고예측 AUC >= 0.7 | PASS / BLOCKED |
