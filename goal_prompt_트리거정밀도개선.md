# /goal 프롬프트: 사고 징조 트리거 정밀도 개선 방안 탐색

아래 내용을 `/goal` 커맨드의 `$ARGUMENTS`로 사용.

---

## 목표

현재 트리거 시스템의 **오탐 구조적 원인**을 진단하고, 실제 사고 징조만 포착하는 개선 방안을 설계한다. 구현이 아닌 **방안 탐색과 설계**가 목표.

## 문제 정의

55분(v2) + 33분(v3) 실 가동 결과, 녹화된 영상 전부가 **정상 교통 흐름**이었다.

```
v2: 트리거 167건 → 녹화 4건 → 보존 0건 (ITS 미확인, 영상 확인 = 정상)
v3: 트리거 157건 → 녹화 6건 → 보존 0건 (영상 확인 = 정상 교통)
```

트리거 13건의 메타데이터 (전체 기록):
```
T1 sev=0.82 | TTC=0.5s    → 정상 (v2, 필터 전)
T1 sev=0.12 | TTC=2.7s    → 정상
T1 sev=0.06 | TTC=2.8s    → 정상
T1 sev=0.97 | TTC=0.1s    → 정상 (bbox 겹침)
T1 sev=0.99 | TTC=0.0s    → 정상 (bbox 겹침)
T1 sev=0.99 | TTC=0.0s    → 정상 (bbox 겹침)
T1 sev=0.70 | TTC=0.9s 2f → 정상 (v3)
T1 sev=0.85 | TTC=0.4s 2f → 정상
T1 sev=0.84 | TTC=0.5s 2f → 정상
T1 sev=0.89 | TTC=0.3s 2f → 정상
T1 sev=0.51 | TTC=1.5s 2f → 정상
T3 sev=0.33 | 정지 3s      → 정상 (정체)
T4 sev=0.92 | 역주행       → 정상 (추적 오류)
```

**전부 오탐. 실제 사고 징조는 0건.**

## 근본 원인 분석

### 원인 1: TTC가 2D 픽셀 공간에서 계산됨

현재 TTC 계산 (`ttc_calculator.py`):
```python
# bbox 중심 간 유클리드 거리 (픽셀)
dist = math.hypot(cx2 - cx1, cy2 - cy1)
# 거리 감소율 → closing_speed (px/s)
closing_speed = (dist_prev - dist_curr) / dt
# TTC = dist / closing_speed (초)
```

문제:
- **다른 차선** 차량도 2D에서 가까우면 TTC가 낮게 계산
- 고속도로에서 인접 차선 차량은 **항상** 2D 거리가 가까움
- 원근 투영으로 프레임 상단(먼 곳) 차량이 하단(가까운 곳) 차량보다 조밀
- 결과: 정상 주행 = TTC 0.3~0.9s로 상시 발화

### 원인 2: 차선(lane) 인식 없음

`compute_all_ttc()`는 **모든 트랙 쌍** O(n²)을 계산:
```python
for tid_a, tid_b in combinations(active_ids, 2):
    ttc = compute_ttc(tracks[tid_a], tracks[tid_b])
```

- 같은 차선 차량만 충돌 가능한데, 다른 차선 차량끼리도 계산
- 6대 차량 → 15쌍, 이 중 같은 차선은 2~3쌍뿐

### 원인 3: 캘리브레이션 없음

`SpeedEstimator`는 `SPEED_FALLBACK_SCALE = 0.05 m/px` 사용:
- 호모그래피 없음 → 픽셀 거리 ≠ 실세계 거리
- 프레임 상단 1px = 실제 ~0.5m, 하단 1px = 실제 ~0.05m
- TTC의 절대값이 무의미

### 원인 4: 트리거 독립 판정

- T1(TTC), T3(정지), T4(역주행) 각각 독립적으로 발화
- 실제 사고 전에는 **여러 징조가 동시에** 나타남 (급감속 + TTC 급락 + 후속 정지)
- 단일 트리거만으로 녹화 시작 → 오탐률 높음

## 탐색해야 할 개선 방향

### 방향 A: 차선 기반 TTC 필터링

- 두 차량의 bbox가 **이동 방향 기준 횡방향으로 겹침**이 있을 때만 TTC 계산
- 고속도로 CCTV 특성상 차선은 대략 수직(또는 대각선) → 횡방향 = X축 근사
- bbox의 X축 범위 겹침(IoU-X) > 임계값 → 같은 차선 추정

### 방향 B: 궤적 이상 탐지 (TTC 대체)

- TTC 대신 **궤적 패턴 변화**를 감지:
  - 급정거: 속도 > 60km/h → 0km/h (5프레임 이내)
  - 급차선변경: 횡방향 이동 > bbox 폭 × 2 (3프레임 이내)
  - 충돌 후 산개: 여러 트랙이 동시에 비정상 방향으로 이동
- 장점: 캘리브레이션 불필요, 2D에서도 상대적 변화로 판단 가능

### 방향 C: 다중 트리거 합의 (Consensus)

- 단일 트리거로 녹화 시작하지 않음
- **N초 윈도우 내 M종 이상 트리거** 동시 발화 시에만 녹화
- 예: T1(TTC) + T2(급감속) 또는 T1 + T3(정지) → 5초 내 동시 → 녹화
- 사고 전에는 여러 징조가 겹치므로 정밀도 향상

### 방향 D: 카메라별 호모그래피 캘리브레이션

- 각 CCTV에 대해 호모그래피 행렬(3×3) 산출
- 차선 표시, 차량 크기 등을 기준점으로 반자동 캘리브레이션
- TTC를 실세계 거리(m) 기반으로 계산
- 가장 정확하지만 **카메라당 수동 작업** 필요

### 방향 E: 임계값 대폭 강화 (단기 대책)

- TTC 임계값: 3.0s → 1.0s
- 연속 프레임: 2 → 4 (1fps 기준 4초간 위험 지속)
- severity 게이트: 0.3 → 0.5
- 다중 트리거 합의와 병행 가능

### 방향 F: 시각적 단서 활용

- 브레이크 등 점등 감지 (적색 영역 변화)
- 차량 파편/잔해 감지 (새로운 정지 객체)
- 연기/화재 감지
- 이건 추가 모델 학습이 필요하므로 장기 과제

## 해야 할 것

1. 각 방향의 **기술적 실현 가능성** 평가 (현재 코드/인프라 기준)
2. **구현 난이도 × 효과** 매트릭스 작성
3. 단기(즉시 적용) / 중기(1~2주) / 장기(수개월) 로드맵 분류
4. 단기 대책 1~2개 선정하여 구체적 설계안 작성
5. 기존 코드 수정 범위 + 예상 영향도 정리

## 평가 기준

- **정밀도(Precision)**: 녹화한 영상 중 실제 사고 징조 비율 → 현재 0%, 목표 50%+
- **재현율(Recall)**: 실제 사고를 놓치지 않는지 → ITS API로 사후 검증
- **오탐률(FPR)**: 시간당 불필요 녹화 횟수 → 현재 ~6건/시간, 목표 1건/시간 이하
- **구현 비용**: 코드 변경량, 추가 데이터/모델 필요 여부

## 참조 파일

| 파일 | 역할 |
|------|------|
| `src/layer1_vision/ttc_calculator.py` | TTC 계산 (현재 2D 픽셀 공간) |
| `src/layer1_vision/trigger_detector.py` | 트리거 7종 판정 (T1~T7) |
| `src/layer1_vision/speed_estimator.py` | 속도 추정 (캘리브 없음, 0.05 m/px 근사) |
| `src/layer1_vision/vision_pipeline.py` | 통합 파이프라인 (YOLO → ByteTrack → 트리거) |
| `src/layer0_data/run_realtime.py` | 실시간 수집 파이프라인 (녹화/검증/보존) |
| `src/config_new.py` | 트리거 임계값 설정 |

## 제약

- CPU 전용 (GPU 없음)
- 사고 영상 학습 데이터 없음 (지금 모으는 중)
- CCTV별 캘리브레이션 데이터 없음
- 1fps 분석 (프레임 간 시간 간격 1초)

## 산출물

1. 개선 방향별 실현 가능성 평가표
2. 구현 난이도 × 효과 매트릭스
3. 단기/중기/장기 로드맵
4. 단기 대책 구체 설계안 (코드 수준)

## 하지 말 것

- 코드 구현 (설계안까지만)
- MLLM 연동 설계
- DuckDB 연동
- 새 모델 학습/데이터셋 구축 계획
