# CCTV 사고분석 시스템 — 진행상황 보고서

**작성일**: 2026-05-15 (설계 회의 + 구현 + 데이터 확보 반영)

---

## 1. 시스템 개요

CCTV 영상에서 교통사고를 자동으로 탐지하고 분석하는 시스템.
핵심 아이디어: **이상징후 탐지(AI) → 사고 확인(API)** 2단계 구조.

```
CCTV 영상 실시간 분석
       ↓
이상징후 감지 (AI 모델)
       ↓
돌발상황 API로 사고 여부 확인
       ↓
확인된 사고 영상 자동 저장 + 학습 데이터 축적
```

기존 방식(API 알림 수신 후 녹화)은 사고 발생과 알림 사이 시간차로 인해
사고 순간을 놓치는 문제가 있었음. AI가 먼저 이상징후를 포착하고,
API로 실제 사고 여부를 확인하는 방식으로 해결.

### 최종 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CCTV 사고분석 시스템                               │
│                                                                         │
│  ┌───────────┐    ┌──────────────────────────────────────────────────┐  │
│  │  CCTV     │    │  Layer 1: 비전 파이프라인                         │  │
│  │  실시간   │───→│  검출 → 추적 → 분류 → 속도 → TTC                 │  │
│  │  영상     │    │         ↓                                        │  │
│  └───────────┘    │  15차원 특성벡터 (매 프레임, 매 차량)              │  │
│                   └─────────────┬────────────────────────────────────┘  │
│                                 │                                       │
│                    ┌────────────┼────────────┐                          │
│                    ▼            ▼            ▼                          │
│  ┌──────────────────┐ ┌─────────────┐ ┌─────────────────┐              │
│  │ VectorDB ①       │ │ 예측 모델    │ │ VectorDB ②      │              │
│  │ 궤적 패턴 DB      │ │ (앙상블)     │ │ 장면 임베딩 DB   │              │
│  │                  │ │             │ │                  │              │
│  │ · 차량별 궤적     │ │ · Rules(L1) │ │ · 프레임 임베딩   │              │
│  │   시퀀스 임베딩   │ │ · IF (L2)   │ │ · 정상 분포 학습  │              │
│  │ · 과거 이상 패턴  │ │ · LSTM (L3) │ │ · 이상도 거리     │              │
│  │   유사도 검색     │ │ · GNN (L4)  │ │   측정            │              │
│  │                  │ │             │ │                  │              │
│  │ [Qdrant/Milvus]  │ │  앙상블 점수  │ │ [Qdrant/Milvus]  │              │
│  └────────┬─────────┘ └──────┬──────┘ └────────┬─────────┘              │
│           │                  │                  │                        │
│           └──────────────────┼──────────────────┘                        │
│                              ▼                                          │
│                    ┌──────────────────┐                                  │
│                    │ 트리거 판정       │                                  │
│                    │ Alert(0.3~0.7)   │                                  │
│                    │ Alarm(≥0.7)      │                                  │
│                    └────────┬─────────┘                                  │
│                             │                                           │
│              ┌──────────────┼──────────────┐                            │
│              ▼              ▼              ▼                            │
│  ┌───────────────┐ ┌──────────────┐ ┌──────────────┐                   │
│  │ 키프레임 선정  │ │ ITS API 확인  │ │ 영상 버퍼    │                   │
│  │ + 메타데이터   │ │ (돌발상황)    │ │ 저장         │                   │
│  └───────┬───────┘ └──────┬───────┘ └──────────────┘                   │
│          │                │                                             │
│          ▼                ▼                                             │
│  ┌──────────────────────────────────────┐                               │
│  │ LLM (MLLM) 분석                      │                               │
│  │                                      │                               │
│  │  입력:                               │                               │
│  │  · 키프레임 이미지 (3~5장)            │   ┌────────────────────────┐  │
│  │  · 차량 궤적/속도/TTC 메타데이터      │   │ VectorDB ③             │  │
│  │  · 이상징후 점수 + 위반 규칙          │◄──│ 사고 사례 DB            │  │
│  │  · 유사 과거 사례 (RAG)              │   │                        │  │
│  │                                      │   │ · 보고서 임베딩          │  │
│  │  출력:                               │   │ · 유사 사례 검색 (RAG)   │  │
│  │  · 사고 유형 판정                     │──→│ · 대응 매뉴얼 매칭       │  │
│  │  · 관련 차량 행동 서술                │   │                        │  │
│  │  · 심각도 등급                        │   │ [Qdrant/Milvus]         │  │
│  │  · 대응 권고                          │   └────────────────────────┘  │
│  └──────────────────┬───────────────────┘                               │
│                     │                                                   │
│                     ▼                                                   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    관계형 DB (PostgreSQL)                         │   │
│  │                                                                  │   │
│  │  ┌─────────────┐  ┌──────────────┐  ┌────────────────────────┐  │   │
│  │  │ cameras     │  │ incidents    │  │ incident_vehicles      │  │   │
│  │  │─────────────│  │──────────────│  │────────────────────────│  │   │
│  │  │ camera_id   │←─│ camera_id    │  │ incident_id (FK)       │  │   │
│  │  │ location    │  │ incident_id  │─→│ track_id               │  │   │
│  │  │ road_name   │  │ timestamp    │  │ vehicle_class          │  │   │
│  │  │ direction   │  │ type         │  │ speed_kmh              │  │   │
│  │  │ lanes       │  │ severity     │  │ trajectory_embedding_id│  │   │
│  │  │ roi_config  │  │ anomaly_score│  │ bbox_sequence (JSONB)  │  │   │
│  │  └─────────────┘  │ api_confirmed│  └────────────────────────┘  │   │
│  │                    │ weather      │                              │   │
│  │                    │ video_path   │  ┌────────────────────────┐  │   │
│  │                    │ report_text  │  │ alerts                 │  │   │
│  │                    └──────┬───────┘  │────────────────────────│  │   │
│  │                           │         │ alert_id               │  │   │
│  │                           │         │ incident_id (FK)       │  │   │
│  │  ┌────────────────────┐   │         │ level (ALERT/ALARM)    │  │   │
│  │  │ model_performance  │   │         │ score                  │  │   │
│  │  │────────────────────│   │         │ trigger_rules          │  │   │
│  │  │ date               │   │         │ response_time_sec      │  │   │
│  │  │ model_version      │   │         │ operator_action        │  │   │
│  │  │ true_positive      │   │         └────────────────────────┘  │   │
│  │  │ false_positive     │   │                                     │   │
│  │  │ false_negative     │   │         ┌────────────────────────┐  │   │
│  │  │ precision          │   │         │ reports                │  │   │
│  │  │ recall             │◄──┘         │────────────────────────│  │   │
│  │  └────────────────────┘             │ report_id              │  │   │
│  │                                     │ incident_id (FK)       │  │   │
│  │                                     │ llm_output (JSONB)     │  │   │
│  │                                     │ operator_revision      │  │   │
│  │                                     │ embedding_id → VDB③   │  │   │
│  │                                     │ created_at             │  │   │
│  │                                     └────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    피드백 루프 (자기강화)                          │   │
│  │                                                                  │   │
│  │  API 확인 결과 ──→ incidents.api_confirmed = true/false          │   │
│  │       │                                                          │   │
│  │       ├─ 실제 사고 (TP) ──→ 양성 라벨 → 예측 모델 재학습         │   │
│  │       │                  ──→ 궤적 임베딩 → VectorDB ① 추가       │   │
│  │       │                  ──→ 장면 임베딩 → VectorDB ② 추가       │   │
│  │       │                                                          │   │
│  │       └─ 오탐 (FP) ──────→ 규칙 threshold 보정                  │   │
│  │                          ──→ 오탐 패턴 임베딩 → VDB② 정상 보강   │   │
│  │                                                                  │   │
│  │  운영자 보고서 수정 ──→ reports.operator_revision                 │   │
│  │       │                                                          │   │
│  │       └─ LLM 프롬프트 개선 + few-shot 예시 → VectorDB ③ 갱신    │   │
│  │                                                                  │   │
│  │  model_performance ──→ 정기 평가 → 재학습 트리거                 │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 저장소 역할 요약

| 저장소                     | 기술           | 저장 대상                          | 용도                                       |
| -------------------------- | -------------- | ---------------------------------- | ------------------------------------------ |
| **VectorDB ①** 궤적 패턴   | Qdrant/Milvus  | 차량별 궤적 시퀀스 임베딩          | 과거 유사 이상 패턴 검색 → 예측 보강       |
| **VectorDB ②** 장면 임베딩 | Qdrant/Milvus  | 프레임 단위 교통 장면 임베딩       | 정상 분포 학습, 이상도 거리 측정 (IF/LSTM) |
| **VectorDB ③** 사고 사례   | Qdrant/Milvus  | LLM 보고서 + 메타데이터 임베딩     | RAG: 유사 사례 검색 → LLM 분석 품질 향상   |
| **관계형 DB**              | PostgreSQL     | 사고 이력, 알림, 보고서, 성능 지표 | 통계/대시보드, 이력 조회, 모델 성능 추적   |
| **파일 스토리지**          | Expansion 15TB | 영상 원본, 키프레임, AI Hub 데이터 | 학습 데이터 원본, 증거 보존                |

### VectorDB ↔ 관계형 DB 연결 관계

```
관계형 DB                              VectorDB
───────────                            ─────────
incidents.incident_id ─── 1:N ───→ VDB① trajectory_embedding
                                       (incident당 관련 차량 궤적 N개)

incidents.incident_id ─── 1:N ───→ VDB② scene_embedding
                                       (incident 전후 프레임 임베딩 N개)

reports.embedding_id  ─── 1:1 ───→ VDB③ report_embedding
                                       (보고서 1건 = 임베딩 1개)

incident_vehicles     ─── 1:1 ───→ VDB① trajectory_embedding
.trajectory_embedding_id               (차량 1대 = 궤적 임베딩 1개)
```

**조회 흐름 예시:**
1. 새 이상징후 발생 → 특성벡터로 VDB① 검색 → 유사 궤적 과거 사례 ID 획득
2. 사례 ID로 관계형 DB 조회 → 당시 사고 유형/심각도/대응 이력 확인
3. VDB③에서 해당 사례 보고서 검색 → LLM에 few-shot 예시로 전달
4. LLM 보고서 생성 → 관계형 DB 저장 + VDB③ 임베딩 추가

---

## 2. 데이터 수집 — 3개 트랙

| 트랙    | 방식                         | 상태                      |
| ------- | ---------------------------- | ------------------------- |
| Track 1 | 공개 데이터셋 (AI Hub)       | **#71566 라벨 확보 완료** |
| Track 2 | 공식 기관 요청 (도로공사 등) | 보류                      |
| Track 3 | 실시간 API 자동 수집         | **완료**                  |

### Track 3 — 실시간 자동 수집 (완료)

사고 알림을 받으면 인근 CCTV 영상을 자동 녹화하는 시스템.

- ITS 국가교통정보센터 API 2종 연동 완료
  - 돌발상황정보: 교통사고 실시간 조회 (테스트 시 4건 확인)
  - CCTV 영상정보: 전국 363대 조회, 스트림 주소 확보
- 사고 위치와 가장 가까운 CCTV 자동 매칭 (테스트: 0.2km 정확도)
- 녹화 방식: 항상 최근 1분을 보관하다가 사고 시 앞뒤 3분 저장

**한계**: API 알림이 사고보다 수 분 늦어 사고 순간은 놓칠 수 있음.
→ 이상징후 탐지 엔진으로 보완 (아래 3장 참조)

### Track 1 — 공개 데이터셋

| 데이터셋      | 출처                        | 규모    | 용도                      | 상태          |
| ------------- | --------------------------- | ------- | ------------------------- | ------------- |
| AI Hub #71566 | 국도 CCTV 비정상주행        | 1.9TB   | 이상징후 모델 학습 (주력) | **라벨 확보** |
| AI Hub #71850 | 지능형 관제 CCTV            | 13GB    | 검증용                    | 신청 필요     |
| AI Hub #597   | 교통사고 영상 (CCTV 부분만) | 3.8TB   | 보완 학습                 | 신청 필요     |
| DoTA (해외)   | ICCV 2021 논문              | 4,677건 | **사용 불가** (링크 만료) | 폐기          |

- DoTA는 유튜브 영상 전부 삭제됨, 구글 드라이브도 속도 제한으로 포기
- 대시캠 데이터는 시점이 달라 사용하지 않음 (CCTV 고정시점만 사용)

#### AI Hub #71566 — 라벨 데이터 분석 결과 (2026-05-15 확보)

승인 → 다운로드 → 분석 완료. 라벨 JSON 1,906,058건 확보.

**데이터 규모:**

| 구분        | Training (TL) | Validation (VL) |          합계 |
| ----------- | ------------: | --------------: | ------------: |
| 정상        |       857,833 |         103,843 |       961,676 |
| 비정상      |       836,469 |         107,913 |       944,382 |
| **총 JSON** | **1,694,302** |     **211,756** | **1,906,058** |

**라벨 JSON 구조:**

```json
{
  "imageInfo": { "fileName", "resolution", "width": 1280, "height": 720 },
  "annotationImage": [{ "lineID", "lineType"(실선/점선), "lineColor", "polyline":[{x,y}] }],
  "annotation": [{ "id", "bbox":[x,y,w,h], "DrivingType", "category", "leftLine", "safetydistance" }],
  "meta": { "cctvID", "fpsNumber", "weather", "roadName", "lines", "direction", "eventstart/stop" },
  "winker": [{ "rightwinker", "leftwinker" }]
}
```

**비정상주행 7유형과 AnomalyEngine 룰 매핑:**

| 비정상 유형              | Training 건수 | 매핑 룰        |
| ------------------------ | ------------: | -------------- |
| 정체구간 차선변경        |       131,551 | R9 군집 급감속 |
| 방향지시등 불이행        |       126,571 | 신규 R11 후보  |
| 차선 물기                |       121,692 | R5 변형        |
| 안전거리 미확보 차선변경 |       120,428 | R4 TTC / R10   |
| 실선구간 차선변경        |       113,833 | R2 변형        |
| 2개 차로 연속변경        |       111,923 | R5 확장        |
| 동시 차로변경            |       111,471 | R5             |

**차량 카테고리 (9종):** 승용차, SUV, 트럭1t, 트럭5t, 덤프트럭, 소형버스, 대형버스, 탱크로리, 특수차량

**클립 명명규칙:** `p{cam}_{YYYYMMDD}_{HHMMSS}_{n1|an1}_{seq}_{lanes}_{frame}.json``
- `n1` = 정상, `an1` = 비정상
- 클립당 평균 42.9프레임, CCTV 8개소 (국도 21번 등)

**저장 위치:** `/media/ybs/Expansion/CCTV차종분류/accident_data/aihub_71566/labels/`

**원천 이미지 다운로드 대기:**
- Validation 원천 (VS): 225GB — 3파일 (VS.z01 + VS.z02 + VS.zip)
- Training 원천 (TS): 1,757GB — 18파일 (TS.z01~z17 + TS.zip)

### Track 2 — 기관 공식 데이터 (보류)

- 도로공사 ITS센터 연구데이터 이용신청 필요 (공문)
- 행정 절차 2~8주 소요 예상

---

## 3. 이상징후 탐지 엔진 — Level 0+1 구현 완료

### 기본 구조

4단계 탐지 모델을 병렬로 실행하고, 결과를 종합 판단.
단순한 규칙부터 시작해서 점진적으로 고도화.

```
영상에서 추출한 차량 데이터
    ├── Level 1: 규칙 기반 (급정거, 역주행 등)            ── 점수 ──┐
    ├── Level 2: 통계 이상 탐지 (Isolation Forest)       ── 점수 ──┤
    ├── Level 3: 시계열 이상 탐지 (LSTM-AE)              ── 점수 ──┼── 종합 판단
    └── Level 4: 차량 관계 이상 탐지 (STGAE, 조건부)      ── 점수 ──┘
```

### 구현 순서와 일정

| 단계    | 내용                                       | 기간  | 누적    | 상태     |
| ------- | ------------------------------------------ | ----- | ------- | -------- |
| Level 0 | 카메라별 ROI/차로 YAML 설정                | 1주   | 1주     | **완료** |
| Level 1 | 규칙 기반 탐지 (10개 규칙)                 | 2주   | 3주     | **완료** |
| Level 2 | 통계 기반 이상 탐지 (Isolation Forest)     | 2주   | 5주     | 다음     |
| Level 3 | 시계열 궤적 분석 (LSTM-AE → Mamba-AE 검토) | 3주   | 8주     | 대기     |
| Level 4 | 차량 관계 이상 탐지 (STGAE, 조건부)        | 2~3주 | 10~11주 | 대기     |

- Level 0+1 구현 완료 — VisionPipeline에 통합, 테스트 6건 통과
- Level 4는 Level 1~3의 성능이 목표에 미달할 때만 착수
- L4 모델: 기존 GNN → **STGAE로 변경** — 코드 완전 공개, 즉시 적용 가능 (아래 상세 설명)

### Level 1 규칙 상세 (10개)

| 규칙 | 이상징후              | 심각도 |
| ---- | --------------------- | ------ |
| R1   | 급정거                | 높음   |
| R2   | 역주행 (3프레임 지속) | 최고   |
| R3   | 장기정지 (30초 이상)  | 보통   |
| R4   | 충돌 시간 임계 (TTC)  | 높음   |
| R5   | 급차로변경            | 높음   |
| R6   | 보행자 차도 진입      | 최고   |
| R7   | 역방향 저속           | 보통   |
| R8   | 급가속                | 낮음   |
| R9   | 3대 이상 동시 급감속  | 최고   |
| R10  | 정지 차량에 접근      | 높음   |

- 규칙은 설정 파일(YAML)로 관리 → 코드 수정 없이 기준값 조정 가능
- 초기 기준값은 교통공학 지식 기반, 이후 AI Hub 데이터로 보정

### 종합 판단 방식

1. 규칙에서 "최고/높음" 판정 → 즉시 알림
2. AI 모델 2개 이상 동의 → 알림
3. AI 모델 1개라도 확신도 90% 이상 → 알림
4. 규칙 "보통" + AI 모델 보강 → 알림

### 알림 체계

| 수준 | 조건          | 동작                             |
| ---- | ------------- | -------------------------------- |
| 주의 | 점수 0.3~0.7  | 기록 저장 + 영상 버퍼 유지       |
| 경보 | 점수 0.7 이상 | API 확인 + 운영자 통보 + DB 저장 |

### 목표 성능

| 지표      | 목표             | 설명                          |
| --------- | ---------------- | ----------------------------- |
| 재현율    | 90% 이상         | 실제 사고 10건 중 9건 탐지    |
| 정밀도    | 30% 이상         | API 확인으로 오탐 자동 필터링 |
| 알림 빈도 | 시간당 10건 미만 | 카메라 1대 기준               |

### 자기 강화 구조

```
이상징후 탐지 → API로 사고 확인 → 확인된 영상을 학습 데이터에 추가
                                → 모델 재학습 → 탐지 성능 향상
```

운영할수록 데이터가 쌓이고 모델이 좋아지는 구조.

### Level 4: STGAE — 차량 관계 이상 탐지 모델

> 논문: "Anomaly Detection in Multi-Agent Trajectories for Automated Driving"
> (CoRL 2021, Gainer et al.)
> GitHub: **againerju/maad_highway** — 학습·추론 코드 전부 공개

#### 모델 선정 과정

Level 4에 쓸 "차량 관계 모델링" 후보를 조사한 결과:

| 모델      | 논문       | 코드 공개        | 이상탐지 전용 | 비고             |
| --------- | ---------- | ---------------- | ------------- | ---------------- |
| **STGAE** | CoRL 2021  | **완전 공개**    | **O**         | **채택**         |
| VT-Former | CVPRW 2024 | X (404)          | X (예측 전용) | 코드 없음        |
| DiffTAD   | 미출판     | X (베이스라인만) | O             | 메인 코드 미공개 |
| PishguVe  | arXiv 2023 | O                | X (예측 전용) | 참조용           |

"좋은 논문"보다 **"지금 바로 쓸 수 있는 코드"**를 우선하여 STGAE를 채택.

#### STGAE란 무엇인가

STGAE = **S**patial-**T**emporal **G**raph **A**uto-**E**ncoder
(시공간 그래프 자동 인코더)

```
비유: 퍼즐 맞추기

  정상 주행 장면을 "퍼즐"이라고 생각하자.

  [학습]  정상 장면 수만 개를 퍼즐로 분해했다가 다시 맞추기를 반복.
          AI가 "정상 퍼즐은 이렇게 맞추면 된다"를 학습한다.

  [탐지]  새 장면이 들어오면 → 퍼즐로 분해 → 다시 맞추기 시도.
          정상이면 → 퍼즐이 잘 맞춰짐    → 이상 점수 낮음 ✓
          비정상이면 → 퍼즐이 안 맞춰짐  → 이상 점수 높음 ⚠️
                     "이런 패턴은 본 적 없는데?"

  핵심: 비정상 사례를 외우는 게 아니라,
        "정상이 어떤 것인지"를 학습해서 그에서 벗어나면 이상으로 판단.
```

#### 처리 흐름 (4단계)

```
[1단계] 그래프 구성 — 차량 관계를 지도로 그리기
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  CCTV 화면에 차량 5대가 보인다.
  각 차량을 점(노드)으로, 가까운 차량끼리 선(엣지)으로 연결.

    차량A ──── 차량B        이 "지도"를 매 프레임마다 만든다.
      │  \                  30프레임 분량(약 3초)을 묶으면
    차량C   차량D            시간에 따라 관계가 변하는
              │              "움직이는 지도"가 된다.
            차량E

  비유: 사람 5명이 방에 있을 때, 누가 누구 옆에 있는지
        관계도를 매초 그리는 것.


[2단계] 인코더 — 압축하기
━━━━━━━━━━━━━━━━━━━━━━━

  이 움직이는 지도(30프레임 × 5대)를 AI가 압축한다.
  "핵심 패턴"만 남기고 나머지는 버린다.

    30프레임 × 5대 × (x,y) = 300개 숫자
          ↓ 압축
    잠재 벡터 = 수십 개 숫자

  비유: 3분짜리 교통 상황을 한 줄로 요약하는 것.
        "5대가 같은 방향, 비슷한 속도, 일정 간격으로 주행 중"


[3단계] 디코더 — 복원하기
━━━━━━━━━━━━━━━━━━━━━━━

  압축된 요약에서 원래 궤적을 복원 시도.

    잠재 벡터 (수십 개 숫자)
          ↓ 복원
    30프레임 × 5대 × (x,y) = 300개 숫자 (복원본)

  정상이면: 원본과 복원본이 거의 같다 → "이 패턴은 잘 안다"
  비정상이면: 원본과 복원본이 크게 다르다 → "이런 건 본 적 없다"


[4단계] 점수 — 재구성 오차 = 이상 점수
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  원본과 복원본의 차이를 숫자로 환산.

  정상 장면:
    원본: A→(100,200), B→(300,210), ...
    복원: A→(101,199), B→(299,211), ...
    차이: 평균 1.5px → 이상 점수 0.1 ✓

  사고 직전:
    원본: A→(100,200), B→(100,205), ...  ← 두 대가 수렴!
    복원: A→(105,210), B→(295,215), ...  ← "정상이면 이럴 텐데"
    차이: 평균 95px → 이상 점수 0.9 ⚠️
```

#### 왜 "정상만 학습"해도 되는가

```
비유: 맞춤법 검사기

  맞춤법 검사기는 "올바른 문장"만 학습한다.
  틀린 문장의 목록을 외우는 게 아니라,
  올바른 패턴을 학습해서 "이건 이상하다"를 판단한다.

  STGAE도 마찬가지:
  · 정상 주행 궤적 수십만 건을 학습
  · "차들이 이렇게 움직이면 정상이다"를 학습
  · 학습한 적 없는 비정상 패턴 → 복원 실패 → 높은 이상 점수

  장점: 비정상 데이터가 적어도 (또는 없어도) 학습 가능
  우리 데이터: AI Hub #71566 정상 96만건으로 바로 학습 시작 가능
```

#### 우리 시스템에서의 역할

```
Level 1 (규칙):   "차량 1대가 급정거했다"             → 개별 이상
Level 2 (통계):   "이 속도는 통계적으로 비정상"        → 통계 이상
Level 3 (시계열): "이 차의 궤적이 과거와 다르다"       → 시간 이상
Level 4 (STGAE):  "5대의 관계 패턴을 복원할 수 없다"   → 관계 이상
```

L1~L3은 차량을 **하나씩** 본다. STGAE는 **동시에 여러 대의 관계**를 본다.
3대가 각각 약간 감속 → L1은 "정상"이라고 판단 (급정거 아님).
STGAE는 "3대가 동시에 감속하면서 한 지점으로 수렴하는 패턴은 복원 불가" → 이상 판정.

#### 모델 상세

| 항목        | 값                         | 설명                                        |
| ----------- | -------------------------- | ------------------------------------------- |
| 모델 구조   | Graph Conv + GRU           | 공간(그래프) + 시간(순환) 처리              |
| 입력        | `[batch, time, agents, 2]` | 차량별 (x, y) 좌표 시퀀스                   |
| 학습 손실   | Bivariate Gaussian NLL     | 위치의 확률 분포로 재구성                   |
| 학습 데이터 | **정상 주행만**            | 비정상 라벨 필요 없음 (비지도)              |
| 추론 속도   | 수백 ms/장면               | ~30프레임, ~10대 기준. 슬라이딩 윈도우 운용 |
| 코드        | **PyTorch, 완전 공개**     | train/test/one-class 모드                   |

#### 우리 데이터와의 호환

```
우리 VisionPipeline 출력:     STGAE 입력:
  (frame, track_id, cx, cy)  →  [batch, time, agents, (x,y)]

변환 코드 (개념):
  for t in range(30):                     # 30프레임 윈도우
      for i, vehicle in enumerate(tracked_vehicles):
          tensor[t, i, 0] = vehicle.cx    # x 좌표
          tensor[t, i, 1] = vehicle.cy    # y 좌표
```

AI Hub #71566 데이터도 직접 사용 가능:
- `annotation[].bbox` → 차량 중심점 (cx, cy) 추출
- `정상/` 폴더 96만건 → 학습 데이터
- `비정상/` 폴더 94만건 → 검증/임계값 보정

#### 모델 비교 — 검토했던 후보들

|               | STGAE (채택)            | VT-Former               | DiffTAD                   |
| ------------- | ----------------------- | ----------------------- | ------------------------- |
| **원리**      | 현재를 복원 못하면 이상 | 미래를 예측 못하면 이상 | 노이즈 제거 실패하면 이상 |
| **코드**      | 완전 공개               | 없음 (404)              | 메인 미공개               |
| **논문 검증** | CoRL 2021 (검증 완료)   | CVPRW 2024 (예측만)     | 미출판                    |
| **이상탐지**  | 전용 설계               | 별도 설계 필요          | 전용 설계                 |
| **차량 관계** | 그래프로 직접 모델링    | 토큰으로 변환           | Transformer 내부          |
| **CPU 추론**  | 가능                    | 가능 (구현 시)          | 느림 (Diffusion)          |
| **장점**      | 즉시 사용 가능          | 소형 (13만 파라미터)    | AUC +26%                  |
| **단점**      | 2021년 모델             | 코드 없음               | 코드 없음, CPU 느림       |

VT-Former와 DiffTAD는 아이디어는 우수하나 코드가 없어 직접 구현해야 한다.
STGAE는 2021년 모델이지만 코드가 완전하고 이상탐지 전용 설계라 즉시 적용 가능.
향후 VT-Former나 DiffTAD 코드가 공개되면 STGAE와 앙상블하거나 교체 검토.

#### 향후 개선 경로

```
단기:  STGAE 그대로 적용
       → AI Hub #71566 정상 데이터로 학습
       → L1~L3 앙상블에 L4 점수 추가

중기:  PishguVe(GIN+Attention)의 그래프 구조를
       STGAE의 인코더에 이식 → 관계 표현력 강화

장기:  DiffTAD/VT-Former 코드 공개 시
       STGAE와 앙상블 또는 교체 검토
```

---

## 4. 구현된 코드

### Layer 0 — 데이터 수집 (`src/layer0_data/`)

| 파일                       | 기능                                           |
| -------------------------- | ---------------------------------------------- |
| `config.py`                | API 주소, 저장 경로, 품질 기준, AI Hub 파일 맵 |
| `track3_api_incident.py`   | 실시간 사고 감지 (ITS 돌발상황 API)            |
| `track3_cctv_stream.py`    | CCTV 검색 + 영상 녹화 (Ring Buffer)            |
| `track3_auto_collector.py` | 감지→녹화→검증 자동화 통합                     |
| `track1_bridge.py`         | AI Hub #175 다운로드 도구                      |
| `track1_aihub71566.py`     | AI Hub #71566 다운로드/병합/상태 도구          |
| `quality_gate.py`          | 영상 품질 자동 검증                            |

### Layer 1 — 비전 파이프라인 (`src/layer1_vision/`)

| 파일                 | 기능                                                    |
| -------------------- | ------------------------------------------------------- |
| `vision_pipeline.py` | 검출→추적→분류→속도→TTC→트리거→이상징후 통합 파이프라인 |

### AnomalyEngine — 이상징후 탐지 (`src/anomaly_engine/`)

| 파일                 | 기능                                                          |
| -------------------- | ------------------------------------------------------------- |
| `roi_config.py`      | Level 0: 카메라별 ROI/차선 YAML 설정                          |
| `feature_store.py`   | 15차원 특성벡터 추출 + 순환 버퍼 (5프레임 이동평균)           |
| `rules_default.yaml` | 10개 규칙 선언적 정의 (R1~R10)                                |
| `rule_engine.py`     | Level 1: YAML 규칙 평가기 (single/pair/group/special)         |
| `ensemble.py`        | 4단계 계층적 앙상블 판정기                                    |
| `alerter.py`         | 2단계 Alert/Alarm + 속도 제한 (10건/시/스트림)                |
| `engine.py`          | 통합 오케스트레이터 (FeatureStore→Rules→Ensemble→Alerter)     |
| `test_engine.py`     | 통합 테스트 6건 (정상/급정거/역주행/장기정지/군집급감속/통계) |

---

## 5. 발급된 API 키

| 서비스                           | 용도                    | 발급일     |
| -------------------------------- | ----------------------- | ---------- |
| 한국도로공사 (data.ex.co.kr)     | 교통량/속도 보조 데이터 | 2026-05-14 |
| ITS 국가교통정보센터 (its.go.kr) | 돌발상황 + CCTV 영상    | 2026-05-14 |

---

## 6. 모델 실험 결과 (2026-05-20, VS 21만장)

### Level 2 — Isolation Forest

| 지표 | 값 |
| ---- | --- |
| 데이터 | 4,934 클립 (정상 2,467 / 비정상 2,467) |
| 특성 | 27차원 (속도·가속도·heading·횡이동·차간거리·면적변화·차선변경률) |
| ROC-AUC | **0.602** |
| 비정상 recall | 10.8% |

**비정상 서브타입별 탐지율:**

| 비정상 유형 | 탐지율 |
| ----------- | ------ |
| 실선구간 차선변경 | 30.3% |
| 차선 물기 | 13.6% |
| 정체구간 차선변경 | 12.2% |
| 안전거리 미확보 차선변경 | 8.0% |
| 방향지시등 불이행 | 5.1% |

**핵심 특성 (상관계수 Top 5):** area_change(0.53), lateral_speed(0.37), speed(0.36)

### Level 3 — LSTM AutoEncoder

| 지표 | 값 |
| ---- | --- |
| 시퀀스 | 정상 38,352 / 비정상 42,655 (SEQ_LEN=20) |
| 특성 | 12차원 (cx,cy,w,h,dx,dy,speed,heading_sin/cos,lateral,area_chg,ar) |
| 분리도 | **2.25x** (비정상 recon error / 정상) |
| ROC-AUC | 0.477 |

**분석:** bbox 좌표만으로는 "방향지시등 불이행" 같은 시각적 차이 포착 불가. 실제 영상 픽셀 정보(방향지시등 ON/OFF, 차간거리 실측)가 필요.

---

## 7. 미결 사항

1. ~~AI Hub #71566 원천 이미지 다운로드~~ → VS 완료, TS 진행 중 (z03/16)
2. AI Hub #71850, #597 신청
3. 카메라 영상의 실제 거리 환산 방법 (px → meter)
4. 돌발상황 API 호출 제한 확인
5. ~~AI Hub #71566 차량 카테고리 매핑~~ → 매핑 코드 완성

---

### Level 4 — STGAE (학습 중, 2026-05-20)

| 지표 | 값 |
| ---- | --- |
| 데이터 | train 1,973 정상 / test 494 정상 + 2,467 비정상 |
| 세그먼트 | ~54K (obs_len=15 슬라이딩 윈도우) |
| 입력 | 7-column TSV: frame_id track_id agent_id x y major minor |
| 모델 | MSE loss, n_stgcnn=1, n_cnn=5, kernel=3 |
| 학습 | 100 epochs, batch=64, SGD lr=0.01 + StepLR |
| 상태 | **학습 진행 중** (CPU, 수 시간 예상) |

---

## 8. 다음 단계

1. **TS 다운로드 완료 대기** — 백그라운드 진행 중 (z03/16 다운로드 중)
2. ~~**Level 4 STGAE 셋업**~~ → **학습 진행 중** (CPU 백그라운드)
3. **영상 기반 특성 추출** — 방향지시등/차선이탈 등 픽셀 레벨 특성 (VS 이미지 활용)
4. **R1~R10 규칙 보정** — 실데이터 threshold 튜닝
5. **Level 2 재학습** — TS 1.7TB로 풀스케일 학습
6. **Track 3 상시 수집 시작** — 데몬 가동하여 메타데이터 축적
