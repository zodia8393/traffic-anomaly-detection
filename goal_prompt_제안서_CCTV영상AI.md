# /goal 프롬프트: 제안서 12·13장 내용 정리

아래 내용을 `/goal` 커맨드의 `$ARGUMENTS`로 사용.

---

## 목표

「교통센터 생성형 AI 상황관제」 사업계획서 **12장(CCTV 영상 기반 교통상황 판단 기능)**, **13장(영상 기반 생성형 AI 모델 및 검증)** 에 들어갈 내용을 정리한다.

우리가 이미 구현·검증한 코드를 근거로, 제안서에 쓸 수 있는 **기술 내용 정리 문서**를 산출한다.

## 산출물

- 마크다운 문서 1개 (`docs/제안서_12_13장_내용정리.md`)
- 12장, 13장 각각에 대해: 기능 설명, 처리 흐름, 핵심 기술, 검증 결과를 정리
- PPT 슬라이드 제작 하지 않음 — 내용 원고만

## 배경

### 사업계획서 전체 목차

```
 8. 사고데이터 정형 DB화 및 표준 데이터 셋
 9. 정형 DB 모델 및 검증
10. 사고처리시간 예측 학습데이터 DB화
11. 학습데이터, 실제 사고처리 예측에 활용되는 변수, DB 모델 및 검증
─── ↑ 텍스트 LLM (Gemma3) 영역 ↑ ───
12. CCTV 영상 기반 교통상황 판단 기능          ← 우리 담당
13. 영상 기반 생성형 AI 모델 및 검증            ← 우리 담당
─── ↓ 이후 ↓ ───
14. 사고자료 기반 생성형 AI 및 사고처리시간 예측
15. 교통상황 영향 분석 및 예측
```

- 8~11장: **텍스트** 사고자료 → Gemma3 NLP → 정형 DB (이미 작성됨)
- 12~13장: **영상** 사고현장 → 멀티모달 VLM → 정형 DB (우리가 정리)
- 발주처: 한국도로공사 교통센터 교통상황실

### 발주처의 문제 인식 (사업계획서 1장에서 발췌)

> 사고발생 시 CCTV 영상 확인을 통한 정체길이 판단 및 대응 — 근무자의 주관성 및 정확도 측면에서 편차 발생

## 기술 근거: 구현 완료된 코드

### 12장 관련 — CCTV 영상 수집·판단

| 기능 | 설명 | 코드 |
|------|------|------|
| ITS 돌발정보 연동 | /map/outbreak 자동 스캔 → 사고/고장 필터 → 인근 CCTV 순회 | browser_stream.py |
| 체류형 캡처 | CCTV당 30초×1fps 연속 캡처, 사고 후 경과시간 추적 | capture_dwelling() |
| 3-Tier 전국 감시 | Tier1 프리필터(313대) → Tier2 핫스팟(50대) → Tier3 정밀(동적) | run_nationwide.py |
| Vision 파이프라인 | YOLO → ByteTrack → 트리거 7종 (TTC, 급감속, 정지 등) | vision_pipeline.py |
| 프리필터 | MOG2 전경비율 + 프레임차분 → 이상 시 Tier3 승격 | prefilter.py |
| ITS 사고 반응기 | 돌발상황 API 폴링 → 인근 CCTV 자동 편입 | incident_reactor.py |

### 13장 관련 — 멀티모달 AI 분석·DB 적재

| 기능 | 설명 | 코드 |
|------|------|------|
| 멀티모달 VLM | Qwen2.5-VL-3B, CPU 추론, transformers 백엔드 | mllm_client.py |
| 사고 분석 프롬프트 | 도공 전면차단 보고서 실물 양식 기반 설계 | prompts.py (OUTBREAK_REPORT_PROMPT) |
| 대표 프레임 선정 | 히스토그램 거리 기반 30장→5장 자동 선별 | OutbreakMLLMWorker |
| 비동기 분석 큐 | 체류 완료 → 큐 → MLLM 워커 스레드 → JSON 저장 | OutbreakMLLMWorker |
| DB 적재 | DuckDB accidents 35개 컬럼, 자동 마이그레이션 | db_schema.py, metadata_writer.py |

### E2E 흐름

```
ITS 돌발정보 스캔 → 사고 CCTV 클릭 → 30초 체류 캡처
  → 프레임 저장 + metadata.json
  → 대표 5장 선정 → Qwen2.5-VL 추론
  → 도공 보고서 양식 JSON → DuckDB INSERT
```

### MLLM 출력 필드

```
기상 / 차단유형(전면·부분·갓길) / 차단 차로 / 차로수 / 도로선형
관련차량(대수·차종·크기·적재물·파손상태)
화재·전복·적재물유출 / 시설물 피해
사고원인 추정 / 경위 서술 / 심각도 / 신뢰도
```

### 검증 수치

| 항목 | 수치 |
|------|------|
| CCTV 캡처 성공률 | 100% (돌발정보 경로) |
| 프레임 수집 | 1fps × 30초 = 30프레임/CCTV |
| MLLM 추론 | ~15-30초/건 (CPU, 3B, 5장) |
| YOLO 처리 | 21ms/프레임 (CPU) |
| 메모리 | ~124MB (경량 캡처 모드) |
| DB 컬럼 | 35개 (기존 24 + outbreak 11) |

### 참고 자료

| 파일 | 내용 |
|------|------|
| (사업계획서) v0.2.pptx | 기존 슬라이드 톤·구조 참고 |
| 전면차단 샘플.hwp | 도공 보고서 실물 양식 |
| 사고 데이터 표준화.xlsx | 도공 사고 데이터 표준 59~71컬럼 |

## 정리 방향

### 12장: CCTV 영상 기반 교통상황 판단 기능

정리할 내용:
1. **현황 문제**: 상황근무자 육안 CCTV 확인 → 주관적, 편차, 지연
2. **해결 방안**: ITS 돌발정보 연동 + CCTV 자동 순회 + AI 영상 분석
3. **핵심 기술**: 3-Tier 감시, 체류형 캡처, Vision 파이프라인
4. **처리 흐름**: 사고 감지 → CCTV 자동 수집 → 영상 분석 → 상황 판단
5. **기대 효과**: 객관적·실시간 교통상황 판단, 사고 대응 시간 단축

### 13장: 영상 기반 생성형 AI 모델 및 검증

정리할 내용:
1. **Gemma3과의 차별점**: 텍스트 LLM vs 멀티모달 VLM, 상호 보완 관계
2. **모델**: Qwen2.5-VL (멀티모달, CCTV 영상 직접 입력)
3. **출력 설계**: 도공 전면차단 보고서 양식 매핑 → 구조화 JSON
4. **DB 적재**: 분석 결과 → DuckDB 자동 적재 (35컬럼)
5. **검증**: 캡처율, 추론시간, 필드 커버리지 수치

## 하지 말 것

- PPT 슬라이드 제작 금지 — 내용 원고만 정리
- 코드 직접 노출 금지
- 구현 안 된 기능 추가 금지 (검증된 것만)
- 영문 기술용어 남발 금지 — 교통센터 실무자 대상
- Gemma3 내용 수정/대체 금지 — 별개 영역

## 성공 기준

1. 12장·13장 각각 기능 설명 + 처리 흐름 + 핵심 기술 + 검증 결과가 정리됨
2. Gemma3(텍스트)과 Qwen2.5-VL(영상)의 상호 보완 관계가 명확히 서술됨
3. 발주처 문제 인식("근무자 주관성 편차")에 대한 해결 방안으로 연결됨
4. 모든 수치와 기술 내용이 실제 구현·검증 근거에 기반함
